# Mementum State

Project: scry

## Current state

- Initial scry scaffolding has been committed.
- Munera and Mementum have been initialized for future task and project memory tracking.
- User-facing README and AI-agent guidance have been added.
- A top-level `SKILL.md` has been created so agents can copy or reference scry usage guidance in other projects.
- Task `001-result-format-by-invocation-scope` is closed. It changed `scry` results to scoped, configurable formatting: suite scope is compact, single namespace scope includes all assertions without output, and single var scope includes assertions plus output. `CHANGELOG.md`, `README.md`, `AGENTS.md`, and `SKILL.md` document the change.
- Task `002-kaocha-tests-edn-suite-selection` design follow-up from inconsistency review is complete: string suite selectors now have explicit fallback text matching semantics in `design.md`.
- Task `002-kaocha-tests-edn-suite-selection` plan-review follow-up is complete: `plan.md` now pins Kaocha adapter test location/alias command and temporary-project `user.dir` isolation strategy; corresponding review-added steps are checked in `steps.md`.
- Task `002-kaocha-tests-edn-suite-selection` plan inconsistency review found no new actionable feedback; the note is recorded in `implementation.md`.
- Task `002-kaocha-tests-edn-suite-selection` latest design inconsistency follow-up is complete: `:suite` is consistently documented as the supported single-suite form, and `src-kaocha/scry/kaocha.clj` now rejects invalid plural `:suites` values (scalar/string/map/empty) with `ex-info`.
- Task `002-kaocha-tests-edn-suite-selection` plan/steps have been refreshed from the stable design: `plan.md` includes invalid `:suites` API-error handling, and `steps.md` includes validation and test checklist items for invalid plural suite selectors.
- Task `002-kaocha-tests-edn-suite-selection` latest plan ambiguity follow-up is complete: `plan.md` now documents passing resolved suite id values directly to `kaocha.config/apply-cli-args` without stringification, and `test/scry/kaocha_test.clj` covers preservation of exact namespace-qualified keyword ids and exact string ids.
- Task `002-kaocha-tests-edn-suite-selection` implementation is complete: `scry.kaocha/run` loads `tests.edn` from `user.dir`, supports `:suite`/`:suites` selection for loaded, supplied, and fallback configs, preserves full `:config`, applies quiet runtime defaults, and ignores skipped Kaocha suites in scry summaries/results. Focused Kaocha adapter tests and core `scry/run` verification pass. Implementation review and test review found no new actionable issues. README, AGENTS.md, and SKILL.md document the REPL-first Kaocha API.
- Task `002-kaocha-tests-edn-suite-selection` docs-review follow-up is complete: `CHANGELOG.md` Unreleased documents the user-visible Kaocha adapter changes, and the review-added steps item is checked.
- Task `002-kaocha-tests-edn-suite-selection` code-shaper review found no new actionable code-quality issues; the review note is recorded in `implementation.md`.
- Task `002-kaocha-tests-edn-suite-selection` is closed. It made `scry.kaocha/run` load `tests.edn`, support `:suite`/`:suites` selection, preserve full `:config`, and document the REPL-first Kaocha API.
- Task `003-tools-build-jar` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `003-tools-build-jar` ambiguity review found no new actionable ambiguity feedback. Review note is recorded in `implementation.md`; no `design-steps.md` was created because there were no follow-up items.
- Task `003-tools-build-jar` inconsistency review found no new actionable inconsistency feedback. Review note is recorded in `implementation.md`; no `design-steps.md` was created because there were no follow-up items.
- Task `003-tools-build-jar` plan and steps have been created from the stable design. The plan chooses a conservative core-only jar from `src`, with `src-kaocha` excluded to preserve the optional Kaocha boundary; implementation slices cover build alias/script, focused build checks, AGENTS.md docs, and verification.
- Task `003-tools-build-jar` plan ambiguity review found actionable follow-up: specify where focused build checks live and their command, and decide/document whether the optional `install` task is in scope with verification or deferred.
- Task `003-tools-build-jar` plan ambiguity follow-up is complete: `plan.md` and `steps.md` now pin focused build checks to `test/scry/build_test.clj` with the exact `clojure -M:test:build -e ...` command, and the optional local Maven `install` task is explicitly deferred so this task implements only `clean` and `jar`.
- Task `003-tools-build-jar` plan inconsistency follow-up is complete: `plan.md` no longer says the build alias supports optional `install`; it now lists only `clean` and `jar` as in-scope and keeps local Maven installation deferred.
- Task `003-tools-build-jar` implementation is complete: `deps.edn` has a `:build` alias, `build.clj` implements `clean` and a core-only `jar` for coordinate `org.hugoduncan/scry` with version `0.1.<git-revcount>`, `target/` is ignored, `AGENTS.md` documents maintainer build workflow, and focused build checks in `test/scry/build_test.clj` verify version, pom, jar path, included core files, and excluded repo/Kaocha files. Core, build, and Kaocha verification pass.
- Task `003-tools-build-jar` implementation review found no new actionable implementation-quality issues. Review note is recorded in `implementation.md`; core tests, focused build checks, and `clojure -T:build jar` were rerun successfully.
- Task `003-tools-build-jar` test review found one actionable test-quality issue: focused build checks do not cover required clear failure behavior for Git version computation failures/invalid counts, and a review follow-up item was added to make the process boundary injectable/nullable and test those cases without mocks/stubs.
- Task `003-tools-build-jar` test-review follow-up is complete: `build/git-rev-count` now has an optional nullable process boundary, focused build tests cover non-zero Git exits and invalid non-numeric counts with clear `ex-info` assertions, and build/core verification passes. A subsequent test review found no new actionable test-quality issues; focused build checks, core tests, and `clojure -T:build jar` pass.
- Task `003-tools-build-jar` test-shaper follow-up is complete: focused build tests now assert the generated pom excludes the optional Kaocha dependency (`lambdaisland/kaocha`) as well as excluding Kaocha source entries from the jar. Focused build checks, `clojure -T:build jar`, and core tests pass.
- Task `003-tools-build-jar` latest test-shaper review found no new actionable test-quality issues; focused build checks pass and the review note is recorded in `implementation.md`.
- Task `003-tools-build-jar` docs review found one actionable docs issue: `CHANGELOG.md` Unreleased does not mention the new tools.build jar workflow/artifact; a follow-up item was added to `steps.md`.
- Task `003-tools-build-jar` docs-review follow-up is complete: `CHANGELOG.md` Unreleased now documents the `clojure -T:build jar` workflow for `org.hugoduncan/scry`, Git-derived `0.1.<git-revcount>` versions, and core-only packaging with optional Kaocha exclusion.
- Task `003-tools-build-jar` code-shaper review found no new actionable code-quality issues; focused build checks and core tests pass, and the review note is recorded in `implementation.md`.
- Task `003-tools-build-jar` is closed. It added a `tools.build` jar workflow for coordinate `org.hugoduncan/scry`, versioned as `0.1.<git-revcount>`, with a core-only artifact that excludes the optional Kaocha adapter.
- Task `004-github-ci-tests` has been created to add a GitHub Actions workflow running core tests, Kaocha adapter tests, focused build checks, and the jar build on PRs and pushes to `master`.
- Task `004-github-ci-tests` design now requires `actionlint` validation; `mise.toml` provides actionlint for local workflow linting.
- Task `004-github-ci-tests` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `004-github-ci-tests` ambiguity follow-up is complete: `design.md` now clarifies that `actionlint` is required as local/static validation during implementation and is not required as a GitHub Actions CI job/step for this task; the review-added item is checked in `design-steps.md`.
- Task `004-github-ci-tests` inconsistency review found no new actionable inconsistency feedback. Review note is recorded in `implementation.md`; no new design follow-up items were added.
- Task `004-github-ci-tests` plan and steps have been created from the stable design. The plan uses a simple single-job GitHub Actions workflow with full-history checkout, pinned Java/Clojure setup, sequential core/Kaocha/build/jar verification, developer-visible docs, and local `actionlint` validation.
- Task `004-github-ci-tests` plan ambiguity review found actionable follow-up: specify the exact setup action/version pinning policy for Java and Clojure CLI setup, since the plan currently says pinned but not whether major tag, full version tag, or SHA is expected.
- Task `004-github-ci-tests` plan ambiguity follow-up and implementation are complete: `plan.md` now specifies `actions/setup-java@v4` with Temurin JDK 21 and `DeLaGuardo/setup-clojure@13.4`; `.github/workflows/ci.yml` runs core tests, Kaocha adapter tests, focused build checks, and `clojure -T:build jar` on PRs and pushes to `master`; AGENTS.md and CHANGELOG.md document the CI workflow; `actionlint`, core tests, Kaocha tests, build checks, and jar build pass locally.
- Task `004-github-ci-tests` plan inconsistency review found one actionable follow-up: remove or update the stale opening `implementation.md` sentence saying "No implementation yet." now that later notes and steps show implementation is complete.
- Task `004-github-ci-tests` plan inconsistency follow-up is complete: `implementation.md` no longer opens with "No implementation yet.", and all checklist items in `steps.md` are checked.

- Task `004-github-ci-tests` implementation review found one actionable issue: Clojure CI test steps currently print result maps but do not force a non-zero process exit when tests/checks report failures or errors, so GitHub Actions could pass a failing test run. A follow-up item was added to `steps.md`.
- Task `004-github-ci-tests` implementation-review follow-up is complete: `.github/workflows/ci.yml` now exits non-zero when core `scry/run` returns `:pass? false` or when Kaocha/build `clojure.test/run-tests` results are not `ct/successful?`; `actionlint`, core tests, Kaocha tests, build checks, and jar build pass locally.
- Task `004-github-ci-tests` follow-up implementation review found no new actionable implementation-quality issues; `actionlint`, core tests, focused Kaocha tests, focused build checks, and jar build pass locally.
- Task `004-github-ci-tests` test-shaper review found no new actionable test-quality issues; `actionlint`, core tests, focused Kaocha adapter tests, and focused build checks pass locally.
- Task `004-github-ci-tests` docs review found no new actionable documentation issues; README remains public-API focused, AGENTS.md documents CI maintenance guidance, CHANGELOG.md documents the developer-visible CI workflow, and no `doc/` directory is present.
- Task `004-github-ci-tests` code-shaper review found no new actionable code-quality issues; the single-job workflow is simple and locally comprehensible, result-aware Clojure checks fail non-zero, and `actionlint` passes.
- Task `004-github-ci-tests` is closed. It added GitHub Actions CI for PRs and pushes to `master`, running core tests, focused Kaocha adapter tests, focused build checks, and the jar build with full Git history for Git-derived versioning.
- Task `012-add-public-installation-docs` is closed. README now has public Installation docs for `org.hugoduncan/scry` and optional `org.hugoduncan/scry-kaocha`, with copyable `:test` / `:kaocha` aliases using `"RELEASE"`, Git-count `0.1.N` pinning guidance, aligned CLI/Kaocha commands, and adapter transitive-dependency guidance.
- Task `013-update-readme-public-status` is closed. README Status now accurately describes scry as initial public alpha / pre-1.0, notes the documented core runner/API, CLI, scoped results, nested capture isolation, build/release automation, and optional Kaocha adapter as usable/tested, and avoids implying 1.0 API/result-shape stability.
- Task `014-add-public-pom-metadata` is closed. `build.clj` now emits public Maven POM metadata for both `org.hugoduncan/scry` and `org.hugoduncan/scry-kaocha`, including project descriptions, GitHub URL/SCM with deterministic `v${version}` tags, EPL-2.0 license metadata, and Hugo Duncan maintainer/developer metadata. Focused build tests verify filesystem and jar-embedded POMs for both artifacts while preserving dependency and packaging boundaries; focused build checks and `clojure -T:build jars` pass.
- Task `015-run-public-history-secret-audit` is closed. The scoped public-history audit covered current `HEAD` plus reachable local branches/tags/remotes/stash; after adding narrow `.gitleaksignore` fingerprints for three audit-note false positives, `mise exec -- gitleaks detect --source . --redact --log-opts='--all'` exits 0 with no leaks found. Manual grep/history plus Munera/Mementum/.psi review found no blocking sensitive material; the repository is safe to make public from a secrets/history perspective within the declared scope.
- Task `005-release-workflow-and-bb-task` has been created to add release automation: Babashka release task(s) for dry-run dispatch plus stamping changelog/tagging/pushing, and a GitHub Actions release workflow for dry-run verification, tag-triggered verification, Clojars deploy, and GitHub Release creation. The design draws simplified inspiration from `../../psi/psi-main` while preserving scry's core-only jar and `0.1.<git commit count>` versioning.
- Task `005-release-workflow-and-bb-task` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `005-release-workflow-and-bb-task` ambiguity review follow-up is complete: `design.md` now pins dry-run local/remote ref-to-commit matching and exact expected-version computation, bracketed release changelog heading/extraction rules, and strict publishing tag format validation for `v0.1.<git-count>` tags with nonconforming `v*` tags failing before publish.
- Task `005-release-workflow-and-bb-task` plan ambiguity review found three actionable follow-ups: pin the focused release helper/task test command and classpath/alias; specify the deploy task credential/artifact contract; and decide the workflow dispatch ref/SHA input contract with an explicit checked-out-SHA verification.
- Task `005-release-workflow-and-bb-task` plan ambiguity follow-up is complete: `plan.md` now pins focused release helper tests to `test/scry/release_test.clj` with `:release-test` classpath including `bb/`, specifies `clojure -T:build:deploy deploy` credentials/artifact contract, and chooses separate release workflow dry-run inputs `ref`, `sha`, and `expected_version` with checked-out SHA verification.
- Task `005-release-workflow-and-bb-task` Slice 1 implementation is complete: `bb/scry/release.clj` contains Babashka-compatible helper logic for Git-count version math, strict release tag validation/agreement, changelog stamping/extraction, dry-run ref agreement, clean-master checks, GitHub origin parsing, and existing-tag retry planning; `test/scry/release_test.clj` covers these helpers through the `:release-test` alias, and focused release helper tests pass.
- Task `005-release-workflow-and-bb-task` Slice 2 implementation is complete: `bb.edn` exposes `release` and `release:tag`; `bb release --dry-run [--ref REF]` validates clean local/remote state, GitHub origin, `gh`, workflow presence, exact SHA/ref agreement, computes expected Git-count version, and dispatches release workflow inputs; `bb release:tag` stamps changelog/commits/tags with off-by-one versioning; `bb release` reuses/pushes existing tags or creates and pushes the release. Focused release tests and basic bb task loading pass.
- Task `005-release-workflow-and-bb-task` Slice 3 implementation is complete: `deps.edn` has a maintainer-scoped `:deploy` alias for `slipset/deps-deploy`; `build/deploy` runs as `clojure -T:build:deploy deploy`, requires `CLOJARS_USERNAME`/`CLOJARS_PASSWORD`, builds the same core-only jar first, and deploys the generated jar plus pom through deps-deploy. Focused build checks cover credential failure, deploy argument shape, and preservation of the core-only/Kaocha exclusion boundary.
- Task `006-kaocha-support-jar-release-artifact` has been created to add a separate optional Kaocha adapter jar (preferred coordinate `org.hugoduncan/scry-kaocha`) and include it in release builds, Clojars deploys, and GitHub Release artifacts while keeping the core jar free of Kaocha code/dependencies.
- Task `006-kaocha-support-jar-release-artifact` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `006-kaocha-support-jar-release-artifact` ambiguity review follow-up is complete: `design.md` now keeps `deploy` core-only and requires a combined `deploy-all` release task, pins adapter Kaocha dependency metadata to the development `:kaocha` alias source, and requires separate core/adapter class-pom output directories with explicit jar/pom path retention.
- Task `006-kaocha-support-jar-release-artifact` plan ambiguity review found three actionable follow-ups: pin standalone `kaocha-jar` clean/stale-output semantics, choose/document the Kaocha dependency source-of-truth mechanism and drift check, and specify exact non-overlapping release workflow jar matching patterns.
- Task `006-kaocha-support-jar-release-artifact` plan ambiguity follow-up is complete: `plan.md` now chooses deterministic `deps.edn` `:kaocha` alias reading as the single source of truth for the adapter pom's Kaocha dependency version, pins standalone `kaocha-jar` to delete only adapter-specific outputs while preserving existing core jar output, and specifies non-overlapping release workflow jar patterns with exact-one-match failure semantics. The review-added items in `steps.md` are checked.
- Task `006-kaocha-support-jar-release-artifact` plan inconsistency review found no new actionable inconsistency feedback; the note is recorded in `implementation.md`.
- Task `006-kaocha-support-jar-release-artifact` is closed. It added a separate optional `org.hugoduncan/scry-kaocha` adapter jar, combined `jars` and `deploy-all` build/release support, two-artifact release workflow integration, and docs/tests while keeping the core jar free of Kaocha code/dependencies.
- Task `007-command-line-progress-results` has been created to add command-line-specific behavior: print `.` for passing test vars, print unqualified failing/erroring test names, clear/write `.scry-results/` EDN files for failures/errors, print summary counts, and preserve existing REPL/API behavior. The initial CLI open questions are resolved: support both `-m` and `-X` via common implementation, all selectors, live test-var progress, `.edn` files with namespace-prefixed names, empty `.scry-results/` after passing runs, full detail including stdout/stderr, preferably Kaocha mode, stdout for dots/summary, and stderr for failing/erroring names. Exit code must be `0` only when all selected/executed test vars pass; failures, errors, unknown status, runner/argument errors, or zero executable tests must exit non-zero.
- Task `007-command-line-progress-results` ambiguity review follow-up is complete: `design.md` now pins the shared `-m`/`-X` option normalization model, makes Kaocha CLI mode mandatory when the optional adapter is available, and specifies the shared `run-cli` outcome plus `-main`/`-X` process-exit behavior. All review-added items in `design-steps.md` are checked.
- Task `007-command-line-progress-results` design inconsistency review found actionable follow-up: clarify the per-runner selector contract for Kaocha versus core selectors, including any `:dirs`/`:test-paths` mapping, and clarify that Kaocha result-file output follows the adapter's merged stdout/stderr behavior unless a new separation mechanism is required.
- Task `007-command-line-progress-results` design inconsistency follow-up is complete: `design.md` now states core mode supports `:dirs`, `:namespaces`, `:vars`, and `:ns-pattern`; Kaocha mode rejects core-only namespace/var/ns-pattern selectors, maps `:dirs` / `--dir` only to fallback `:test-paths`, and preserves the adapter's merged-output `:out` plus empty `:err` result-file semantics.
- Task `008-testing-without-mocks-repl-strategy` has been created to apply the Testing Without Mocks pattern language to scry's REPL verification strategy: define focused core/Kaocha/build/release REPL slices, avoid broad discovery as the primary all-green check, make long-lived REPL runs deterministic, and document nullable infrastructure boundaries for unavoidable external effects.
- Task `008-testing-without-mocks-repl-strategy` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `008-testing-without-mocks-repl-strategy` ambiguity review found actionable follow-up: decide the canonical REPL slice mechanism, pin exact alias/evaluation forms, decide CI broad-discovery versus focused-slice strategy, choose temp namespace cleanup/isolation, and inventory accepted Nullable infrastructure boundaries versus mock-like seams.
- Task `008-testing-without-mocks-repl-strategy` ambiguity follow-up is complete: `design.md` now chooses documentation-only explicit REPL snippets, pins core/Kaocha/build/release alias contexts and forms, directs CI toward focused slices with broad discovery only as smoke coverage, requires unique generated temp namespaces plus `remove-ns`, and inventories accepted Nullable infrastructure boundaries versus mock-like seams.
- Task `008-testing-without-mocks-repl-strategy` inconsistency follow-up is complete: `design.md` now requires `.github/workflows/ci.yml` implementation to make an explicit focused core slice the primary core CI signal, with any broad `(scry/run)` only a separately named fresh-process smoke check, and requires `AGENTS.md` implementation to present focused REPL slices as canonical while removing or qualifying broad `(scry/run)` as smoke/discovery-only.
- Task `008-testing-without-mocks-repl-strategy` plan and steps have been created from the stable design. The plan chooses documentation-only explicit REPL snippets, focused CI core slicing, temporary Kaocha namespace uniqueness/removal, Nullable boundary audit, and focused slice verification as the ordered implementation path.
## Active focus

- Task `018-add-quickdoc-api-docs` has been created to add `borkdude/quickdoc` based API reference documentation at `doc/API.md`, including a reproducible regeneration command, curated public API surface for `scry.core`, `scry.cli`, and optional `scry.kaocha`, README linkage, maintainer guidance, and verification notes.
- Task `018-add-quickdoc-api-docs` architecture-review follow-up is complete: `design.md` now requires quickdoc/tooling dependencies to remain maintainer/docs-only via a dedicated docs alias, Babashka task, or equivalent non-runtime mechanism; forbids adding them to top-level runtime deps or published core/Kaocha dependency metadata; and still requires composing the optional Kaocha classpath for documenting `scry.kaocha`. The review-added `design-steps.md` item is checked.
- Task `018-add-quickdoc-api-docs` ambiguity follow-up is complete: `design.md` now pins the generated var-level API surface (`scry.core` REPL/API vars plus advanced `last-run`, `scry.cli/run` only for public `-X`, and optional `scry.kaocha/run` plus advanced `result->scry`), chooses `bb api-docs` with `bb api-docs --check` for no-diff verification, and requires generated notes such as pre-1.0 public-alpha and optional Kaocha classpath guidance to come from source-controlled generation config/code rather than hand edits.
- Task `018-add-quickdoc-api-docs` inconsistency follow-up is complete: `design.md` now requires generated `scry.cli/run` docs to show only the user-facing `[opts]` arity and hide the `io-boundary` implementation/test seam through deterministic source-controlled generation input, and requires generated main-style CLI examples to use README-aligned alias forms such as `clojure -M:test -m scry.cli` and optional `clojure -M:test:kaocha -m scry.cli --runner kaocha ...`. The review-added `design-steps.md` items are checked.
- Task `018-add-quickdoc-api-docs` plan and steps have been refreshed from the stable design. The plan uses `bb api-docs` / `bb api-docs --check`, keeps quickdoc docs-only, composes the optional Kaocha classpath for generation, curates the exact public API vars/arity, and orders slices through quickdoc orientation, API audit, tooling integration, generated content, docs links, and verification.
- Task `018-add-quickdoc-api-docs` plan-ambiguity follow-up is complete: `plan.md` now pins `bb/scry/api_docs.clj` (`scry.api-docs`) as the shared source-controlled generator entry point for both `bb api-docs` and `bb api-docs --check` through `clojure -M:quickdoc:kaocha -m scry.api-docs`, and pins focused build/POM checks plus `deps.edn` diff inspection as the concrete docs-tooling dependency-boundary verification. Focused build checks pass (6 tests, 176 assertions).
- Task `018-add-quickdoc-api-docs` plan inconsistency review found one actionable follow-up: `plan.md` now pins a concrete docs-only `deps.edn` `:quickdoc` alias for generator classpath/tooling, while `steps.md` still allowed older generic docs dependency mechanisms. A follow-up item was added to align the step with the plan-pinned alias.
- Task `018-add-quickdoc-api-docs` plan-inconsistency follow-up is complete: `steps.md` now aligns Slice 3 with the plan-pinned `deps.edn` `:quickdoc` alias that adds `bb/` plus pinned quickdoc to the generator classpath; the review-added item is checked. Older unchecked implementation steps remain pending for the implementation pass.
- Task `018-add-quickdoc-api-docs` implementation is complete: `deps.edn` now has a docs-only `:quickdoc` alias, `bb api-docs` / `bb api-docs --check` drive `bb/scry/api_docs.clj`, generated `doc/API.md` documents only the curated public `scry.core`, `scry.cli/run`, and optional `scry.kaocha` API surface, README/AGENTS/CHANGELOG are updated, and quickdoc remains out of runtime/build/POM dependency surfaces. Verification passes: `bb api-docs --check`, `bb clj-fmt:check`, `bb clj-kondo:lint`, focused core slice, optional Kaocha adapter/CLI slice, focused build/POM checks, and `git diff --check`.
- Task `018-add-quickdoc-api-docs` implementation-review follow-up is complete: generated `scry.cli/run` prose now includes the `:error` key in the documented non-zero `ex-info` contract alongside `:summary` and `:outcome`; `doc/API.md` was regenerated, and `bb api-docs --check`, formatting, linting, and `git diff --check` pass.
- Task `018-add-quickdoc-api-docs` follow-up implementation review found no new actionable implementation-quality issues. Reviewed generator/tooling/docs changes and reran API-doc, load, focused core/Kaocha/build, formatting, linting, dependency-boundary, and diff checks successfully.
- Task `018-add-quickdoc-api-docs` test review found one actionable test-quality issue: API-doc generation/check verification proves reproducibility but lacks automated focused assertions for the curated API-doc content contract (included public vars/arities/prose and omitted implementation namespaces/helpers). A follow-up item was added to `steps.md`.
- Task `018-add-quickdoc-api-docs` test-review follow-up is complete: `test-quickdoc/scry/api_docs_test.clj` plus a focused `:quickdoc-test` alias now assert committed API docs match generated markdown, include the curated `scry.core`, `scry.cli/run` `[opts]`, and optional `scry.kaocha` public surface, and omit implementation namespaces, CLI helpers, and the hidden `io-boundary` arity. Focused API-doc content checks, formatting, linting, `bb api-docs --check`, focused build/POM checks, and `git diff --check` pass.
- Task `018-add-quickdoc-api-docs` follow-up test-review item is complete: the focused API-doc content regression now asserts the generated intro/prose contract for the pre-1.0 public-alpha note, README relationship, `bb api-docs`, and `bb api-docs --check`, while retaining optional Kaocha classpath prose coverage. Focused API-doc content checks, formatting, linting, `bb api-docs --check`, and `git diff --check` pass.
- Task `018-add-quickdoc-api-docs` latest test review found one actionable test-quality issue: the focused API-doc content regression is not wired into CI or maintainer API-doc final-verification guidance, so curated API-doc surface/prose regressions remain manual-only unless reviewers remember the bespoke `:quickdoc-test` command. A follow-up item was added to `steps.md`.
- Task `018-add-quickdoc-api-docs` latest test-review follow-up is complete: the focused API-doc content regression is now part of the automated/documented maintainer path. CI runs `bb api-docs --check` plus `clojure -M:quickdoc:quickdoc-test:kaocha ... scry.api-docs-test`, and AGENTS.md documents both as API-doc final verification. Verification passed: `bb api-docs --check`, focused API-doc content regression (1 test, 44 assertions), `mise exec -- actionlint .github/workflows/ci.yml`, and `git diff --check`.
- Task `018-add-quickdoc-api-docs` follow-up test review found one actionable test-quality issue: the quickdoc docs-only dependency boundary is still guarded by manual diff/grep plus generic build/POM checks, not by an automated focused assertion that quickdoc remains only under the `:quickdoc` alias and absent from runtime deps, generated POMs, and packaged artifacts. A follow-up item was added to `steps.md`. Review verification passed: `bb api-docs --check`, focused API-doc content regression (1 test, 44 assertions), and focused build/POM checks (6 tests, 176 assertions).
- Task `018-add-quickdoc-api-docs` dependency-boundary test-review follow-up is complete: focused build tests now assert `io.github.borkdude/quickdoc` appears only under the docs-only `:quickdoc` alias and is absent from generated core/Kaocha POMs and packaged jar entries. Verification passed: focused build/POM checks (7 tests, 183 assertions), `bb api-docs --check`, focused API-doc content regression (1 test, 44 assertions), formatting, linting, and `git diff --check`.
- Task `018-add-quickdoc-api-docs` follow-up test review found one actionable test-quality issue: existing automated dependency-boundary checks assert where quickdoc appears, but do not assert the design-required explicit quickdoc version/tag+SHA pinning remains non-floating. A follow-up item was added to `steps.md`; review verification passed for `bb api-docs --check`, focused API-doc content regression, and focused build/POM checks.
- Task `018-add-quickdoc-api-docs` quickdoc-pinning test-review follow-up is complete: focused build tests now assert `io.github.borkdude/quickdoc` stays confined to the docs-only `:quickdoc` alias and is pinned to an explicit non-floating Maven release version or git tag+SHA (current spec `:git/tag "v0.2.6"`, `:git/sha "ce86780"`). Verification passed: focused build/POM checks (7 tests, 184 assertions), formatting, linting, `bb api-docs --check`, focused API-doc content regression, and `git diff --check`.
- Task `018-add-quickdoc-api-docs` exact-surface test-review follow-up is complete: the focused API-doc content regression now asserts the exact allowed var-anchor sets for included `scry.core` and `scry.kaocha` namespace sections, so accidental newly documented helper vars fail the focused check. Verification passed: focused API-doc content regression (1 test, 48 assertions), formatting, linting, `bb api-docs --check`, and `git diff --check`.
- Task `018-add-quickdoc-api-docs` latest implementation review found no new actionable implementation-quality issues. Reviewed the quickdoc generator, generated API reference, focused API-doc regression, CI wiring, dependency-boundary checks, and public docs guidance; API-doc, focused content, focused build/POM, formatting, linting, actionlint, and diff checks pass.
- Task `018-add-quickdoc-api-docs` exact CLI API surface follow-up is complete: the focused API-doc content regression now asserts the exact allowed var-anchor set for the included `scry.cli` namespace is only `scry.cli/run`, matching the `scry.core`/`scry.kaocha` exact-surface checks. A final task test review found no new actionable test-quality issues; API-doc check, focused API-doc content regression, and focused build/POM checks pass.
- Task `016-update-skill-cli-repl-usage` has been created to update top-level `SKILL.md` so it clearly distinguishes REPL/in-process `scry.core/run` usage from command-line `scry.cli` usage, including CLI progress, `.scry-results/` files, exit-code semantics, final-verification guidance, and optional Kaocha CLI examples.
- Task `016-update-skill-cli-repl-usage` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md, README.md, and current SKILL.md plus the task design.
- Task `016-update-skill-cli-repl-usage` ambiguity review found no new actionable ambiguity feedback. Review note is recorded in `implementation.md`; no `design-steps.md` was created because there were no follow-up items.
- Task `016-update-skill-cli-repl-usage` plan and steps have been updated from the stable design. The plan keeps the task documentation-only, treats `README.md` and `AGENTS.md` as authorities, and orders slices through orientation, REPL/API framing, CLI workflow guidance, and documentation-only verification.
- Task `016-update-skill-cli-repl-usage` plan ambiguity review found two actionable follow-ups: decide whether `SKILL.md` frontmatter must be updated so it no longer frames the portable skill as in-process-only, and pin which structured artifacts agents should inspect for `-m` versus `-X` CLI runs instead of scraping progress text.
- Task `016-update-skill-cli-repl-usage` plan-ambiguity follow-up is complete: `plan.md` now chooses to update `SKILL.md` frontmatter during the eventual skill body edit and pins CLI structured artifacts (`.scry-results/*.edn` for `-m`, returned outcome map or non-zero `ex-info` data for `-X`). The review-added items in `steps.md` are checked; main `SKILL.md` implementation remains deferred to the older unchecked steps.
- Task `016-update-skill-cli-repl-usage` plan inconsistency review found one actionable follow-up: `steps.md` had only the completed decision to update `SKILL.md` frontmatter, while `plan.md` now requires actually doing that update during the body edit. An explicit unchecked frontmatter-update implementation step was added.
- Task `016-update-skill-cli-repl-usage` plan-inconsistency follow-up is complete: top-level `SKILL.md` frontmatter now covers both REPL/in-process structured inspection and CLI/final-verification usage (`scry.cli`, process status, `.scry-results`, and no terminal scraping). The newly added follow-up item in `steps.md` is checked; older unchecked body-edit steps remain pending.
- Task `016-update-skill-cli-repl-usage` implementation is complete: top-level `SKILL.md` now explicitly separates REPL/in-process usage for interactive debugging/targeted structured inspection from CLI usage for final verification/CI-style process status, includes core `-m`/`-X` examples, optional Kaocha CLI examples, progress/result-file/summary/exit-code semantics, and structured-artifact guidance for `.scry-results/*.edn` and `-X` outcome/ex-info data. All steps are checked; verification was documentation-only plus `git diff --check`.
- Task `016-update-skill-cli-repl-usage` implementation review found no new actionable implementation-quality issues. Review note is recorded in `implementation.md`; no follow-up steps were added.
- Task `016-update-skill-cli-repl-usage` test review found no new actionable test-quality issues. Review note is recorded in `implementation.md`; no follow-up steps were added because the task is documentation-only and runtime tests are explicitly out of scope.
- Task `016-update-skill-cli-repl-usage` test-shaper review found no new actionable test-quality issues. Review note is recorded in `implementation.md`; no follow-up steps were added because the documentation-only verification surface is focused and proportional.
- Task `016-update-skill-cli-repl-usage` docs review found no new actionable documentation issues. Review note is recorded in `implementation.md`; no follow-up steps were added because `SKILL.md` aligns with README/AGENTS/CHANGELOG and no `doc/` directory is present.
- Task `016-update-skill-cli-repl-usage` code-shaper review found no new actionable code-quality issues. Review note is recorded in `implementation.md`; no follow-up steps were added.
- Task `016-update-skill-cli-repl-usage` is closed. Top-level `SKILL.md` now distinguishes REPL/in-process structured inspection from command-line/final-verification usage, including CLI examples, progress/result-file/exit-code semantics, structured-artifact guidance, and optional Kaocha CLI usage.
- Task `011-add-project-license` implementation is complete: top-level `LICENSE` contains the unmodified EPL-2.0 text from the plan-approved SPDX `license-list-data` fallback pinned to commit `421fbabbe80c94c58c12316af1bc6a2dca2362bc` after the direct SPDX URL returned 404, README has the only required license note, POM metadata remains deferred to `014-add-public-pom-metadata`, and all task steps are checked with exact license comparison and diff-scope verification recorded.
- Task `011-add-project-license` ambiguity-review design follow-up is complete: `design.md` now pins README as the only doc requiring a license note, records the POM metadata handoff in this task's `implementation.md` for `014-add-public-pom-metadata`, and uses the SPDX EPL-2.0 full text as the canonical `LICENSE` source with no project-specific header/footer additions. All review-added items in `design-steps.md` are checked.
- Task `011-add-project-license` inconsistency-review design follow-up is complete: generated Maven/Clojars POM metadata, including license metadata, is now fully out of scope for task 011 and deferred to `014-add-public-pom-metadata`; task 011 only records the handoff note in `implementation.md`. All review-added items in `design-steps.md` are checked.
- Task `011-add-project-license` plan and steps have been refreshed from the stable design. The plan slices implementation into adding the canonical SPDX EPL-2.0 `LICENSE`, adding the README-only license note, and verifying/recording scope and POM metadata handoff boundaries.
- Task `011-add-project-license` plan ambiguity-review follow-up is complete: `plan.md` now pins the only allowed EPL-2.0 retrieval fallback to an SPDX-controlled `spdx/license-list-data` source and requires exact byte-for-byte comparison from a temporary retrieved text, with source/ref and SHA-256 recorded in `implementation.md`. The review-added item in `steps.md` is checked.
- Task `011-add-project-license` plan inconsistency-review follow-up is complete: `steps.md` Slice 1 retrieval wording now names the direct SPDX EPL-2.0 URL and the only allowed SPDX-controlled `spdx/license-list-data` fallback; the review-added checklist item is checked.
- Open task `005-release-workflow-and-bb-task`: latest code-shaper follow-up is complete. `parse-release-args` now rejects `bb release --ref REF` without `--dry-run` with `--ref is only supported with --dry-run`, and focused release argument tests cover accepted/rejected forms. Focused release helper tests pass: 17 tests, 80 assertions, 0 failures, 0 errors. The review-added item in `steps.md` is checked.
- Task `007-command-line-progress-results` implementation-review follow-up is complete: `scry.cli/run` now converts `-X` normalization/argument errors into the same structured `:scry.cli/non-zero` `ex-info` contract as other non-zero CLI outcomes, including `:exit-code 1`, nil `:summary`, and preserved underlying `:scry.cli/argument-error` data in `:error`/`:outcome`. Focused core CLI tests pass (19 tests, 94 assertions).
- Task `007-command-line-progress-results` latest implementation-review follow-up is complete: CLI result files now recursively sanitize raw `Throwable` values into EDN-readable maps with class/message/stacktrace/ex-data/cause detail before `pr-str`, while preserving assertion `:stacktrace` strings. Focused core CLI tests pass (21 tests, 104 assertions), including `clojure.edn/read-string` coverage for error result files; the existing core suite through `scry` passes (61 tests, 281 pass).
- Task `007-command-line-progress-results` latest implementation-review follow-up is complete: Kaocha-mode normalization now rejects namespace-pattern aliases `:namespace-pattern` and `:namespace-regex` the same way it rejects `:ns-pattern`, with focused coverage. Focused core CLI tests pass (22 tests, 116 assertions); focused optional Kaocha CLI tests pass (3 tests, 17 assertions).
- Task `007-command-line-progress-results` latest implementation review found no new actionable implementation-quality issues. Reviewed the CLI implementation, core/Kaocha progress hooks, tests, and docs; focused core CLI tests and optional Kaocha CLI tests pass.
- Task `007-command-line-progress-results` latest test-review follow-up is complete: focused core CLI tests now cover successful non-var selectors end-to-end via explicit namespace selection and directory plus `:ns-pattern` discovery, verifying progress/summary output, empty `.scry-results/`, and canonical executed vars. Focused core CLI tests pass (26 tests, 123 assertions); optional Kaocha CLI tests pass (3 tests, 17 assertions).
- Task `007-command-line-progress-results` latest test-review follow-up is complete: optional Kaocha CLI tests now cover end-to-end `run-cli` execution with an explicit `:config` map, selecting both passing and failing suites and verifying progress, canonical results, `.scry-results/*.edn`, and captured adapter output. Focused optional Kaocha CLI tests pass (4 tests, 29 assertions).

- Task `007-command-line-progress-results` latest test-review follow-up is complete: focused `parse-main-args` tests now cover documented `-m` short/alias flags (`-r`, `-d`, `--ns`/`-n`, `-v`, namespace-pattern aliases, and `-s`). Focused core CLI tests pass (26 tests, 132 assertions); focused optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` latest test-review follow-up is complete: runner-exception handling coverage now exercises the real core runner path by selecting a deliberately missing namespace, instead of injecting a throwing `:run-clojure-test` stub. Focused core CLI tests pass (26 tests, 132 assertions).
- Task `007-command-line-progress-results` latest test-review follow-up is complete: focused core CLI tests now cover a real executed `:unknown` status var (`deftest` with no assertions), verifying stderr progress name, unknown summary count, non-zero exit, no result file, and canonical unknown result data. Focused core CLI tests pass (28 tests, 138 assertions).
- Task `007-command-line-progress-results` latest implementation review found no new actionable implementation-quality issues. Reviewed CLI implementation, core/Kaocha progress hooks, tests, and docs; focused core CLI tests pass (28 tests, 138 assertions) and focused optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` latest test-review follow-up is complete: focused core CLI tests now cover user-supplied `:result-format` projection that omits `:results`, verifying the returned projection is preserved while detailed `.scry-results/*.edn` output still comes from CLI-retained `:canonical-results` with assertions and captured stdout/stderr. Focused core CLI tests pass (30 tests, 149 assertions).
- Task `007-command-line-progress-results` latest test review found no new actionable test-quality issues. Reviewed focused core CLI tests, optional Kaocha CLI tests, CLI fixtures, core/Kaocha progress hooks, and docs; focused core CLI tests pass (30 tests, 149 assertions) and focused optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` latest test-shaper follow-up is complete: focused core CLI tests now cover a real mixed fail+error var end-to-end, verifying error precedence in summary, result-file status, canonical results, stderr progress, and single file output. Focused core CLI tests pass (32 tests, 159 assertions); focused optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` docs-review follow-up is complete: `scry.cli/usage` now shows the supported `clojure -X:test scry.cli/run :runner :clojure-test` key/value invocation form instead of a quoted map literal; focused core CLI tests pass (32 tests, 159 assertions), and `--help` output was manually checked.
- Task `007-command-line-progress-results` code-shaper follow-up is complete: result-directory lifecycle, deterministic result-file naming/writing, and recursive EDN sanitization now live in `scry.cli.results`, while `scry.cli` delegates those concerns and retains parsing/orchestration/progress/entry points. Focused core CLI tests pass (32 tests, 159 assertions); focused optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` latest code-shaper follow-up is complete: `.scry-results/` cleanup now uses `java.nio.file.Files` with `NOFOLLOW_LINKS`, deleting symlink entries without traversing outside the intended result directory. Focused coverage verifies a symlinked `.scry-results` target is preserved while the result directory is recreated. Focused core CLI tests pass (34 tests, 164 assertions); optional Kaocha CLI tests pass (4 tests, 29 assertions).
- Task `007-command-line-progress-results` latest code-shaper follow-up is complete: `parse-main-args` now rejects repeated/conflicting namespace-pattern aliases (`--ns-pattern`, `--namespace-pattern`, `--namespace-regex`) instead of silently keeping the last value, with focused parser coverage. The latest code-shaper review found no new actionable code-quality issues; focused core CLI tests pass (34 tests, 166 assertions), and optional Kaocha CLI tests pass (4 tests, 29 assertions).
- AGENTS.md test workflow guidance now explicitly says to use REPL-driven testing during development, but run documented command-line checks for final verification before handoff/review/closure.
- Task `008-testing-without-mocks-repl-strategy` code-shaper follow-up is complete: `test/scry/kaocha_test.clj` temporary project directories are now allocated through `with-temp-project` and deleted after each test while preserving existing `user.dir` restoration and generated namespace cleanup. Focused `scry.kaocha-test` verification passes (8 tests, 48 assertions).
- Task `008-testing-without-mocks-repl-strategy` latest code-shaper follow-up is complete: generated Kaocha temporary-project namespaces now use shared `scry.temp-ns/unique-ns`, combining a per-JVM UUID run id with a monotonic counter instead of `System/nanoTime`. Focused optional Kaocha verification passes: adapter 8 tests/48 assertions and CLI 4 tests/29 assertions.
- Task `009-replaceable-capture-context` has been created to refactor `scry.capture` around a dynamically replaceable/nullable capture context for nested in-process test runs. The design also includes investigating per-test `*out*`/`*err*` rebinding or equivalent stack-aware routing so output is isolated per test var while preserving `clojure.test` fixture semantics.
- Task `009-replaceable-capture-context` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md plus the task design.
- Task `009-replaceable-capture-context` ambiguity follow-up is complete: `design.md` now specifies nil/disabled context semantics, fixture output ownership, required intended-var allow-listing/ignored non-owned nested frames, and the same-thread/cooperative dynamic-binding boundary; the initial review-added `design-steps.md` items are checked.
- Task `009-replaceable-capture-context` design inconsistency follow-up is complete: the stale Planning question that presented intended-var allow-listing as optional/follow-up work now asks how to implement the required allow-list and ignored-frame stack with minimal capture-result changes; the review-added `design-steps.md` item is checked.
- Task `009-replaceable-capture-context` plan and steps have been created from the stable design. The plan slices the work through baseline characterization, capture context primitives, owned/ignored frame semantics, core runner integration with per-var output, nested core coverage, Kaocha isolation, CLI regression checks, and documentation/final verification.
- Task `009-replaceable-capture-context` plan ambiguity review found actionable follow-up: pin refactored capture helper API/arity semantics, clarify `:each` fixture output ownership before/after `test-var` events without duplicate counts/progress, and decide how pre-refactor characterization/red tests are introduced without leaving committed failing tests.
- Task `009-replaceable-capture-context` plan ambiguity follow-up is complete: `plan.md` now pins capture helper compatibility API/arity semantics, per-var `:each` fixture output ownership/progress timing through a local `test-vars` loop, and the red-test workflow; review-added `steps.md` items are checked.
- Task `009-replaceable-capture-context` plan inconsistency review found one actionable follow-up: Slice 1 still says to add focused failing tests even though the red-test workflow says red-first characterization must be temporary/REPL-only and committed regression tests must pass with the implementation. A follow-up item was added to align the wording.
- Task `009-replaceable-capture-context` plan inconsistency follow-up is complete: `plan.md` Slice 1 and the related nested-core test step now explicitly require red-first characterization to be temporary/REPL-only and committed regression tests to be added only with the implementation that makes them pass.

- Task `009-replaceable-capture-context` implementation pass is complete: `scry.capture` now has dynamically replaceable/disabled contexts with intended-var allow-listing, owned/ignored frame stacks, and per-var output ownership; `scry.clojure-test/run` installs a fresh context and preserves fixture semantics with a local test-vars loop; `scry.kaocha/run` disables enclosing scry capture while Kaocha executes. Focused core, core CLI, and optional Kaocha adapter/CLI verification pass.
- Task `009-replaceable-capture-context` implementation review found one actionable issue: core `:progress-callback` currently runs inside the per-var capture/output-owner window, so callback writes to `*out*`/`*err*` are appended to the completed var's public output. A follow-up item is recorded in `steps.md`.
- Task `009-replaceable-capture-context` implementation-review follow-up is complete: core progress callbacks now run after the per-var output-owner wrapper restores ownership and inside `capture/without-context`, so callback stdout/stderr pass through to caller/CLI writers instead of public var output while `:each` teardown remains in the progress entry. Focused core slice, focused CLI tests, and `clojure -M:test -m scry.cli` pass.
- Task `009-replaceable-capture-context` latest implementation review found two actionable issues: `:each` fixture assertions before/after `test-var` frames are ignored instead of failing/attaching to the owning var, and `routing-writer` mishandles `Writer.write(String, off, len)` by throwing `ClassCastException`; follow-up items were added to `steps.md`.
- Task `009-replaceable-capture-context` second implementation-review follow-up is complete: `:each` fixture assertions before/after `test-var` are now attributed to the output-owner var and fail the run when failing; `:once` fixture assertions affect run counts while remaining outside public var entries; `routing-writer` now handles `Writer.write(String, off, len)` substring writes correctly. Focused core/capture, core CLI, optional Kaocha adapter/CLI, and `clojure -M:test -m scry.cli` verification pass (CLI: 79 tests, 401 assertions).
- Task `009-replaceable-capture-context` third implementation-review follow-up is complete: CLI assertion summaries now use the runner aggregate `:summary` when available, CLI-retained result formats keep aggregate `:pass?`, and exit-code checks aggregate pass/fail state as well as canonical var statuses. Focused CLI regression coverage confirms failing `:once` fixture assertions produce one passing var progress dot, no result files, a non-zero exit, and assertion counts of 3 pass / 2 fail. Focused core CLI tests pass (36 tests, 172 assertions).
- Task `009-replaceable-capture-context` latest implementation review found one actionable implementation/documentation mismatch: AGENTS.md still says an outer `scry/run` would capture intentional nested Kaocha reports as failures, which is stale after replaceable/disabled capture; a follow-up item was added to update the Kaocha REPL-slice guidance.
- Task `009-replaceable-capture-context` latest implementation-review follow-up is complete: AGENTS.md optional Kaocha REPL-slice guidance now keeps `clojure.test/run-tests` as the deterministic focused recommendation, but no longer claims outer `scry/run` would capture nested Kaocha reports as failures; it explains replaceable/disabled capture prevents that leak while broad outer discovery remains non-canonical.
- Task `009-replaceable-capture-context` fifth implementation-review follow-up is complete: raw nested non-owned `clojure.test/test-vars` calls with namespace `:once` fixture output/assertions now run with scry capture disabled and local writers, preventing fixture events before/after inner `:begin-test-var` from inheriting the enclosing output owner or counts. Focused core, CLI, optional Kaocha, and `clojure -M:test -m scry.cli` verification pass.
- Task `009-replaceable-capture-context` sixth implementation-review follow-up is complete: intentionally failing raw-nested outer helper `deftest`s now live in fixture namespace `scry.fixtures.raw-nested-outer` and are invoked explicitly by scry acceptance wrappers, so plain `clojure.test/run-tests 'scry.clojure-test-test` passes while raw nested isolation remains covered. Plain clojure.test namespace verification, focused core slice, and `clojure -M:test -m scry.cli` pass.

- Task `009-replaceable-capture-context` seventh implementation-review follow-up is complete: any ignored frame on the capture frame stack now suppresses nested owned-frame opening, so a raw nested non-owned `clojure.test/test-var` that invokes an allow-listed helper var no longer leaks helper assertions/output/counts into the outer result. Focused raw-nested coverage, core slice, plain `clojure.test/run-tests 'scry.clojure-test-test`, core CLI, and optional Kaocha adapter/CLI verification pass.
- Task `009-replaceable-capture-context` latest implementation-review follow-up is complete: output-owner writes/assertions are provisional until `:begin-test-var`, so a short-circuiting `:each` fixture no longer creates a public unknown var entry, progress callback, or result file; fixture output is kept non-public while fixture assertions remain in aggregate counts. Focused core, CLI, optional Kaocha, plain clojure.test, and `clojure -M:test -m scry.cli` verification pass.
- Task `009-replaceable-capture-context` latest implementation-review follow-up is complete: README.md no longer claims direct `clojure.test/test-vars` delegation and now describes the local fixture-preserving `clojure.test/test-var` execution loop with preserved `:once`/`:each` semantics and per-var output ownership.
- Task `009-replaceable-capture-context` latest implementation review found one actionable issue: isolated raw nested `clojure.test/run-tests`/`test-vars` no longer leaks into the outer scry capture, but its own inner assertion counters can be lost because the root-redefined report dispatcher ignores events while capture is disabled. A follow-up item was added to preserve inner raw clojure.test summary semantics while keeping outer capture isolated.
- Task `009-replaceable-capture-context` tenth implementation-review follow-up is complete: raw nested non-owned `clojure.test/run-tests` and `test-vars` now run with the original clojure.test report function while scry capture remains disabled, preserving inner pass/fail/error counters without leaking events/output into the outer scry result. Focused raw-nested coverage, core slice, plain clojure.test checks, optional Kaocha checks, and `clojure -M:test -m scry.cli` pass.
- Task `009-replaceable-capture-context` eleventh implementation-review follow-up is complete: singular raw nested non-owned `clojure.test/test-var` now preserves its own clojure.test counters under the original report function while outer scry capture stays isolated. Focused raw-nested coverage, core slice, plain clojure.test checks, optional Kaocha checks, and `clojure -M:test -m scry.cli` pass.
- Task `009-replaceable-capture-context` latest implementation review found no new actionable implementation-quality issues. Reviewed capture/core/CLI/Kaocha implementation, fixture coverage, README/AGENTS guidance, and reran focused core, plain clojure.test, optional Kaocha, and CLI verification successfully.
- Task `009-replaceable-capture-context` test-review follow-up is complete: `fixture-assertion-ownership-test` snapshots and restores all shared `scry.fixtures.asserting-fixtures/*-pass?` atoms in a `finally`, so running the test alone cannot contaminate later fixture checks. Focused single-test restoration, core slice, and `clojure -M:test -m scry.cli` verification pass.
- Task `009-replaceable-capture-context` latest test review found one actionable test-quality issue: `run-cli-run-level-fixture-failures-test` restores shared `scry.fixtures.asserting-fixtures/*-pass?` atoms to hard-coded `true` values instead of snapshotting/restoring prior state, so a follow-up item was added to make it focused-run-safe.
- Task `009-replaceable-capture-context` latest test-review follow-up is complete: `run-cli-run-level-fixture-failures-test` now snapshots/restores all `scry.fixtures.asserting-fixtures/*-pass?` atoms in `finally`; focused non-default restoration probe, focused core CLI tests, and `clojure -M:test -m scry.cli` pass.

- Task `009-replaceable-capture-context` latest test-review follow-up is complete: singular raw non-owned `clojure.test/test-var` coverage now asserts the outer scry var entry has empty `:err`, proving inner raw stderr from `scry.fixtures.output/noisy-and-fails` does not leak into the outer result. Focused raw-nested coverage and `clojure -M:test -m scry.cli` pass.
- Task `009-replaceable-capture-context` latest test-review follow-up is complete: `fixtures-are-honoured-test` now describes scry's local fixture-preserving loop around `clojure.test/test-var` instead of claiming delegation to `clojure.test/test-vars`; the focused test passes.

- Task `009-replaceable-capture-context` docs review found three actionable documentation issues: CHANGELOG.md does not mention the user-visible nested/reentrant capture fix, README.md does not describe nested in-process runner isolation/limits, and top-level SKILL.md still claims `scry.clojure-test` delegates to `clojure.test/test-vars`. Follow-up items were added to `steps.md`.
- Task `009-replaceable-capture-context` docs-review follow-up is complete: CHANGELOG.md Unreleased mentions the nested/reentrant capture fix, README.md documents nested in-process runner isolation and same-thread/cooperative limits, SKILL.md now describes the local fixture-preserving `clojure.test/test-var` loop, and all docs-review steps are checked.
- Task `009-replaceable-capture-context` is closed. It refactored capture around dynamically replaceable/disabled contexts with intended-var allow-listing, owned/ignored frame stacks, per-var output ownership, fixture-preserving local `clojure.test/test-var` execution, raw nested `clojure.test` counter preservation without outer leakage, and Kaocha adapter isolation. Focused core, plain clojure.test, CLI, and optional Kaocha verification passed after implementation/test/docs/code-shaper reviews.

- Task `017-cli-synthetic-errors-and-outcome-kinds` has been created from psi integration feedback. It will fix CLI handling for nil-var synthetic/suite-level Kaocha load errors and add machine-readable CLI outcome classification so callers do not need to parse stderr text for fallback policy.
- Task `017-cli-synthetic-errors-and-outcome-kinds` architecture review found no new actionable architectural-fit feedback. Review note is recorded in `implementation.md`; META.md and doc/architecture.md were absent, so review used AGENTS.md/README.md plus the task design.
- Task `017-cli-synthetic-errors-and-outcome-kinds` ambiguity review found actionable follow-up: pin the exact outcome classification API/key/vocabulary, mixed-signal precedence, synthetic load/suite-error recognition, nil-var progress/result-file naming and collision behavior, and public documentation scope for machine-readable classification.
- Task `017-cli-synthetic-errors-and-outcome-kinds` ambiguity-review follow-up is complete: `design.md` now pins top-level `:scry.cli/outcome-kind` for `run-cli` outcomes and `clojure -X` non-zero ex-data, defines the initial outcome vocabulary and precedence, recognizes load/suite errors structurally from failing/erroring non-var-backed canonical entries after a valid `:canonical-results` vector exists, pins synthetic progress/result-file naming and collision handling, and names README.md/SKILL.md/AGENTS.md/CHANGELOG.md docs updates for implementation. All review-added `design-steps.md` items are checked.
- Task `017-cli-synthetic-errors-and-outcome-kinds` inconsistency review found actionable follow-up: resolve whether `-m` parser errors have structured outcome-kind exposure or only human stderr/non-zero status, and align pass/zero-tests wording so only concrete var-backed entries count as executable tests. Follow-up items were added to `design-steps.md`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` inconsistency-review follow-up is complete: `design.md` now keeps `-m` parser errors process-oriented (non-zero plus human stderr only) while structured outcome-kind classification is available through `clojure -X`/direct CLI APIs, and consistently defines executable tests as concrete var-backed canonical entries only. The review-added `design-steps.md` items are checked.
- Task `017-cli-synthetic-errors-and-outcome-kinds` plan and steps have been refreshed from the stable design. The plan keeps changes localized to CLI progress/result-file/outcome surfaces, uses top-level `:scry.cli/outcome-kind`, preserves normal var-backed filenames/progress, adds deterministic synthetic suite-level names/collision handling, and orders slices through characterization, synthetic identity/files, progress labels, classification, `clojure -X` propagation, docs, and verification.
- Task `017-cli-synthetic-errors-and-outcome-kinds` plan ambiguity review found actionable follow-up: pin whether `:scry.cli/outcome-kind` is authoritative for `:exit-code`, and decide how CLI summary `:tests` counts / `:var-count` treat synthetic non-var-backed entries. Follow-up items were added to `steps.md`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` plan-ambiguity follow-up is complete: `plan.md` makes `:scry.cli/outcome-kind` authoritative for CLI exit code (`:scry.cli/pass` => 0, all other kinds non-zero), requires implementation/tests to avoid the old total-entry `var-count` pass path for synthetic-only passing entries, and preserves existing summary compatibility by counting all canonical result entries in `:summary :tests` / `:var-count` while using concrete var-backed entries only for executable/pass classification.
- Task `017-cli-synthetic-errors-and-outcome-kinds` plan inconsistency review found no new actionable inconsistency feedback; the review note is recorded in `implementation.md`, and no follow-up steps were added.
- Task `017-cli-synthetic-errors-and-outcome-kinds` implementation slices 1-3 are complete: CLI result-file writing and progress now tolerate synthetic/non-var-backed canonical entries, preserve normal var-backed names/progress, assign deterministic synthetic `suite-error-N`/`suite-fail-N` filenames with optional `:ns` prefixes and collision suffixes, and print synthetic fail/error/unknown progress labels instead of throwing on nil/non-concrete `:var`. Focused core CLI tests pass (41 tests, 213 assertions), optional Kaocha CLI tests pass (4 tests, 29 assertions), and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` implementation is complete: CLI result-file/progress handling tolerates synthetic/non-var-backed canonical entries with deterministic `suite-error-N`/`suite-fail-N` names, `run-cli` outcomes and `clojure -X` non-zero ex-data include top-level authoritative `:scry.cli/outcome-kind`, docs in README/SKILL/AGENTS/CHANGELOG describe the additive CLI contract, focused core CLI tests pass (42 tests, 240 assertions), optional Kaocha CLI tests pass (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 517 assertions), and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` implementation-review follow-up is complete: `scry.cli/classify-outcome` no longer uses bare `:pass? false` as test-failure classification, preserving documented precedence so unknown canonical entries classify as `:scry.cli/unknown-result` and no-executable-entry results classify as `:scry.cli/zero-tests` when there are no concrete/aggregate failures. Focused core CLI tests pass (42 tests, 248 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 525 assertions), and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest implementation-review follow-up is complete: CLI canonical result validation now rejects malformed entries with missing or unrecognized `:status` as `:scry.cli/runner-error` before summary/result-file/classification processing, with focused tests for missing and invalid statuses. Focused core CLI tests pass (42 tests, 258 assertions), optional Kaocha CLI tests pass (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 535 assertions), and `git diff --check` passes.

- Task `017-cli-synthetic-errors-and-outcome-kinds` latest implementation-review follow-up is complete: removed the stale unused `result` parameter from `scry.cli/classify-outcome` and updated the call site, preserving outcome classification behavior while making `bb clj-kondo:lint` clean. Verification passed: `bb clj-kondo:lint`, focused core CLI tests (42 tests, 258 assertions), and `git diff --check`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest implementation review found no new actionable implementation-quality issues. Reviewed CLI implementation, synthetic progress/result-file handling, outcome classification, `clojure -X` propagation, focused tests, and docs; `bb clj-kondo:lint`, focused core CLI tests, and optional Kaocha CLI tests pass.
- Task `017-cli-synthetic-errors-and-outcome-kinds` test review found no new actionable test-quality issues. Focused core CLI tests and optional Kaocha CLI tests pass; no follow-up steps were added.
- Task `017-cli-synthetic-errors-and-outcome-kinds` test-shaper follow-up is complete: focused CLI outcome-kind coverage now includes mixed synthetic nil-var load error plus concrete var-backed failure results and asserts `:scry.cli/load-error` takes precedence over `:scry.cli/test-failure`. Focused core CLI tests pass (42 tests, 262 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 539 assertions), `bb clj-kondo:lint` passes, and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper follow-up is complete: synthetic result-file assignment coverage now reserves both a suite-level base filename and its `--2` collision suffix, then asserts the synthetic nil-var error advances deterministically to `loader.demo__suite-error-1--3.edn`. Focused core CLI tests pass (42 tests, 263 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 540 assertions), and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest ambiguity review found no new actionable ambiguity feedback. Reviewed `design.md` against current CLI outcome/progress/result-file docs and relevant CLI code concepts; no follow-up items were added.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper follow-up is complete: `run-exec-entry-point-test` now covers injected synthetic nil-var load-error results through the structured `scry.cli/run` (`clojure -X`) surface, asserting top-level and embedded non-zero ex-data classify as `:scry.cli/load-error` while preserving synthetic result-file data and progress labels. Focused core CLI tests pass (42 tests, 275 assertions), full `clojure -M:test -m scry.cli` passes (85 tests, 552 assertions), and `git diff --check` passes.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest design inconsistency review found no new actionable inconsistency feedback. Reviewed `design.md` against README/SKILL/AGENTS/CHANGELOG CLI docs and relevant CLI/Kaocha code concepts; no follow-up items were added.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest plan ambiguity review found no new actionable ambiguity feedback. Reviewed `plan.md`/`steps.md` against the design, current CLI/result-file/outcome implementation, focused tests, and docs; no follow-up items were added.
- Task `017-cli-synthetic-errors-and-outcome-kinds` current test-shaper follow-up is complete: `.scry-results/` preparation failures are tagged as `:scry.cli/runner-error`, focused CLI coverage asserts create failure yields non-zero runner-error outcome, human stderr, nil result/summary, no result files, and no runner invocation. Verification passed: focused core CLI tests (43 tests, 284 assertions), optional Kaocha CLI tests (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` (86 tests, 561 assertions), `bb clj-kondo:lint`, and `git diff --check`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest plan inconsistency follow-up is complete: `implementation.md` no longer opens with stale "No implementation yet" wording; it now states implementation is complete and that the file records implementation passes, review follow-ups, and verification history. The review-added item in `steps.md` is checked.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper follow-up is complete: focused CLI coverage now injects a synthetic/non-concrete `:unknown`-only canonical result and asserts it classifies as `:scry.cli/unknown-result` before zero-tests, exits non-zero, prints `loader.demo/suite-unknown-1`, writes no result files, and preserves unknown summary counts. Verification passed: focused core CLI tests (43 tests, 291 assertions), full `clojure -M:test -m scry.cli` (86 tests, 568 assertions), optional Kaocha CLI tests (4 tests, 29 assertions), `bb clj-kondo:lint`, and `git diff --check`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` independent implementation pass found no further concrete implementation work; all steps remain checked and focused/final CLI verification still passes (focused core CLI, optional Kaocha CLI, full `clojure -M:test -m scry.cli`, `bb clj-kondo:lint`, and `git diff --check`).
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper review found one actionable test-quality issue: aggregate assertion-failure coverage exercises a concrete passing var plus aggregate fail counts, but not the documented precedence boundary where aggregate fail/error counts exist with no concrete executable canonical entries. A follow-up item was added to cover `:scry.cli/test-failure` precedence over `:scry.cli/zero-tests` for run-level aggregate failures without executable vars.

- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper follow-up is complete: focused CLI coverage now covers aggregate fail/error assertion counts with no concrete executable canonical entries, asserting `:scry.cli/test-failure` takes precedence over `:scry.cli/zero-tests`, exits non-zero, emits aggregate summary output, and writes no result files. Verification passed: focused core CLI tests (43 tests, 297 assertions), optional Kaocha CLI tests (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` (86 tests, 574 assertions), `bb clj-kondo:lint`, and `git diff --check`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` current test-shaper follow-up is complete: focused CLI coverage now exercises `.scry-results/` clear/delete failure during run-start cleanup using a non-writable stale results directory on POSIX-capable filesystems, asserting `:scry.cli/runner-error`, non-zero exit, human stderr, nil result/summary, no result files, no runner invocation, and stale-file preservation until permissions are restored. The coverage is guarded for non-POSIX filesystems. Verification passed: focused core CLI tests (43 tests, 306 assertions), optional Kaocha CLI tests (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` (86 tests, 583 assertions), `bb clj-kondo:lint`, `bb clj-fmt:check`, and `git diff --check`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` code-shaper review found one actionable code-quality issue: CLI progress currently prints a normal passing dot for synthetic/non-concrete `:pass` entries even though those entries are not executable vars and classify as zero-tests; a follow-up item was added to align passing progress with the concrete executable-var model and cover the chosen behavior.
- Task `017-cli-synthetic-errors-and-outcome-kinds` code-shaper follow-up is complete: CLI progress now prints `.` only for concrete var-backed passing entries; synthetic/non-concrete `:pass` entries are silent in progress, preserve summary counts, and classify as `:scry.cli/zero-tests` when no concrete executable vars exist. Focused core CLI tests pass (43 tests, 308 assertions), optional Kaocha CLI tests pass (4 tests, 29 assertions), full `clojure -M:test -m scry.cli` passes (86 tests, 585 assertions), `bb clj-kondo:lint`, `bb clj-fmt:check`, and `git diff --check` pass.
- Task `017-cli-synthetic-errors-and-outcome-kinds` is closed. It fixed CLI handling for synthetic/non-var-backed suite-level results, added additive `:scry.cli/outcome-kind` classification for structured CLI callers, documented the contract, and is represented in PR #1 from branch `scry-improvements`.

## Useful links

- Project README: `README.md`
- Agent guidance: `AGENTS.md`
- Munera task plan: `munera/plan.md`
- Open quickdoc API docs task: `munera/open/018-add-quickdoc-api-docs/`
- Closed CLI synthetic errors/outcome kinds task: `munera/closed/017-cli-synthetic-errors-and-outcome-kinds/`
- Closed public history secret audit task: `munera/closed/015-run-public-history-secret-audit/`
- Closed public POM metadata task: `munera/closed/014-add-public-pom-metadata/`
- Closed README public status task: `munera/closed/013-update-readme-public-status/`
- Closed public installation docs task: `munera/closed/012-add-public-installation-docs/`
- Closed license task: `munera/closed/011-add-project-license/`
- Closed release automation task: `munera/closed/005-release-workflow-and-bb-task/`
- Closed CLI progress/results task: `munera/closed/007-command-line-progress-results/`
- Closed Testing Without Mocks REPL strategy task: `munera/closed/008-testing-without-mocks-repl-strategy/`
- Closed deterministic Kaocha CLI unavailable test task: `munera/closed/010-deterministic-kaocha-cli-unavailable-test/`
- Closed replaceable capture context task: `munera/closed/009-replaceable-capture-context/`
- Closed Kaocha adapter jar task: `munera/closed/006-kaocha-support-jar-release-artifact/`
- Closed GitHub CI task: `munera/closed/004-github-ci-tests/`
- Closed Kaocha suite-selection task: `munera/closed/002-kaocha-tests-edn-suite-selection/`
- Closed build jar task: `munera/closed/003-tools-build-jar/`
- Closed scoped-format task: `munera/closed/001-result-format-by-invocation-scope/`
- Mementum memories: `mementum/memories/`
- Mementum knowledge: `mementum/knowledge/`
- Task `010-deterministic-kaocha-cli-unavailable-test` has been created. It will make `scry.cli-test/run-cli-no-tests-and-runner-errors-test` deterministic when the optional Kaocha adapter is present on the project REPL classpath by adding a minimal internal boundary for optional Kaocha runner resolution and updating the unavailable-adapter test to use that boundary instead of relying on classpath absence.
- All remaining open Munera tasks (`005-release-workflow-and-bb-task`, `007-command-line-progress-results`, `008-testing-without-mocks-repl-strategy`, and `010-deterministic-kaocha-cli-unavailable-test`) were moved to `munera/closed/` by user request; `munera/plan.md` now records no open tasks.
- Public-readiness task `011-add-project-license` is closed. The maintainer chose Eclipse Public License 2.0 (EPL-2.0); the task added canonical EPL-2.0 text as top-level `LICENSE`, added a README license note, and recorded a POM metadata handoff for task `014-add-public-pom-metadata`.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest test-shaper review found one actionable test-shaping issue: the new `.scry-results/` clear/delete failure coverage assumes POSIX file-permission APIs; a follow-up item was added to guard/skip that branch on non-POSIX filesystems while preserving deterministic delete-failure coverage where supported.
- Task `017-cli-synthetic-errors-and-outcome-kinds` latest code-shaper review found no new actionable code-quality issues after the synthetic-pass progress follow-up. Reviewed CLI progress/outcome flow, result-file helpers, focused CLI/Kaocha CLI tests, and docs; focused core CLI tests, optional Kaocha CLI tests, `bb clj-kondo:lint`, and `bb clj-fmt:check` pass. No follow-up steps were added.
