# Implementation Notes

Task created 2026-06-02. No implementation work has started yet.

2026-06-02 architecture review: PASS — design fits current boundaries: keeps scry.core free of Kaocha, preserves clojure.test semantics, treats scoped formatting as a result-shaping concern, and explicitly calls out accessors/docs/tests plus Kaocha reuse/limitations. No new actionable architectural-fit feedback.

2026-06-02 ambiguity review: ACTIONABLE — design leaves the canonical result collection key unclear (`:failures` vs `:results`/both), does not explicitly say whether detailed scoped entries include passing vars, does not define passing entry/status shape, and needs tie-breakers for mixed/filtered invocation options.

2026-06-02 design follow-up pass: completed all ambiguity-review follow-ups in `design-steps.md`. Updated `design.md` to make `:results` the canonical formatted entry collection, preserve `:failures` as compatibility/filtered subset where needed, require detailed single namespace/var scopes to include entries for all executed vars, define `:status` semantics (`:pass`, `:fail`, `:error`, limited `:unknown`), and add scope tie-breakers for mixed `:vars`/`:namespaces` plus filtered non-test vars.

2026-06-02 inconsistency review: PASS — read `design.md`, current README/AGENTS/SKILL docs, and runner/capture/core/Kaocha code. The design is internally consistent after the prior follow-ups; referenced artifacts reflect the current pre-task shape and are explicitly slated for update by the design. No new actionable inconsistency feedback.

2026-06-02 plan ambiguity review: ACTIONABLE — read plan/steps/implementation plus capture, clojure-test, core, Kaocha, tests, README, AGENTS, and SKILL. The plan needs exact precedence for custom `:result-format` flags vs projected key lists, and exact compatibility behavior for `:failures` when configurable top-level keys omit or include result collections.

2026-06-02 review follow-up pass: completed newly added plan-review follow-ups. Updated `plan.md` with the concrete `:result-format` option shape and resolved conflicts by making `:assertions?` and `:output?` authoritative semantic gates applied after `:entry-keys` projection. Also defined custom top-level projection compatibility: `:results` remains canonical and `:failures` remains a compatibility filtered collection when requested, but custom `:top-level-keys` are respected if either/both are omitted; public helpers should prefer `:failures`, fall back to filtered `:results`, then return documented nil/empty values when neither collection is available.

2026-06-02 plan inconsistency review: ACTIONABLE — plan says custom top-level projection is respected and `:failures` appears only when requested, but steps still said to keep `:failures` whenever `:results` is present. Added a follow-up to reconcile the formatting step with the plan's custom `:top-level-keys` behavior.

2026-06-02 review follow-up pass: completed the newly added plan-inconsistency follow-up. Updated `steps.md` so the Slice 4 compatibility item now states that `:failures` is included only when selected/defaulted in `:top-level-keys`, while still remaining a filtered failing/erroring compatibility collection independent of whether `:results` is also included.

2026-06-02 implementation pass: implemented scoped result formatting across the clojure.test runner. `scry.capture` now records `:pass` assertion events, derives per-var status and assertion summaries, builds canonical entries for every executed var, and projects them through per-scope `:result-format` defaults/overrides. `scry.clojure-test` now classifies `:suite`, `:namespace`, and `:var` scope from invocation intent with explicit executable vars taking precedence and non-test var fallback to namespaces. `scry.core` helpers now prefer `:failures`, fall back to filtering `:results`, and tolerate omitted collections. Kaocha output now routes through the shared formatter in suite scope; limitation remains that the adapter defaults to suite scope and Kaocha merges stdout/stderr. Updated README, AGENTS.md, and SKILL.md for canonical `:results`, scoped defaults, and custom formatting. Verification: `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"`; `clojure -M:test:kaocha -e "(require '[scry.kaocha :as k]) ..."`; `clj-kondo --lint src src-kaocha test`.

2026-06-02 implementation review: ACTIONABLE — read task artifacts, implementation, docs, and tests; ran `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"` successfully. Found scope-classification bug: when explicit `:vars` contains no executable test vars and `:namespaces` is supplied, `resolve-vars` returns namespace vars but `result-scope` still classifies from `(seq vars)` and counts the namespace fallback vars, so a one-test namespace is incorrectly treated as `:var` instead of namespace fallback intent.

2026-06-02 implementation follow-up pass: fixed scope classification for explicit `:vars` that resolve to zero executable tests while `:namespaces` supplies fallback executable vars. `result-scope` now counts executable explicit vars directly from the original `:vars` selector instead of counting fallback `vars-to-run`, so namespace fallback uses selector intent. Added assertions that this path gets namespace-format assertion detail and no output keys. Verification: `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"`.

2026-06-02 implementation review: PASS — read task artifacts, implementation, docs, and tests after the namespace-fallback fix; ran `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"`, `clojure -M:test:kaocha -e "(require '[scry.kaocha :as k]) (prn (:pass? (k/run)))"`, and `clj-kondo --lint src src-kaocha test`. No new actionable implementation-quality issues found.

2026-06-02 test review: PASS — read task artifacts, implementation, docs, and tests; applied `.psi/skills/task-test-review/SKILL.md`. Verified tests are well-formed and cover scoped defaults, mixed/filtered scope classification, passing/failing/error assertion detail, custom `:result-format` per scope, public helper compatibility, fixtures, and Kaocha transformation smoke behavior via existing implementation verification. Ran `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"` successfully. No new actionable test-quality issues found.

2026-06-02 test-shaper review: PASS — read `.psi/skills/test-shaper/SKILL.md`, task artifacts, implementation, docs, and test sources; ran `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"` successfully. Tests are focused, deterministic, behavior-oriented, and cover scoped defaults, tie-breakers, assertion/output boundaries, custom formatting per scope, helper compatibility, fixtures, and Kaocha smoke behavior through prior verification. No new actionable test-shaping issues found.

2026-06-02 docs review: ACTIONABLE — read `.psi/skills/review-task-docs/SKILL.md`, task artifacts, README, AGENTS, SKILL, implementation, and tests. README/AGENTS/SKILL accurately describe canonical `:results`, scoped defaults, custom `:result-format`, helper behavior, and the Kaocha suite-scope/output limitation; no `doc/` directory exists. Found one user-visible documentation gap: there is no `CHANGELOG.md` entry/file for this visible result-shape/API behavior change.

2026-06-02 docs follow-up pass: added `CHANGELOG.md` with an Unreleased entry documenting the user-visible scoped result-shape/API behavior change: canonical `:results`, scoped defaults, per-scope `:result-format`, and `:failures` compatibility.

2026-06-02 docs review: PASS — read `.psi/skills/review-task-docs/SKILL.md`, task artifacts, README, AGENTS, SKILL, CHANGELOG, implementation, and tests. README/AGENTS/SKILL/CHANGELOG accurately describe canonical `:results`, scoped defaults, custom `:result-format`, helper behavior, and the documented Kaocha suite-scope/output limitation; no `doc/` directory exists and no other user-facing docs are referenced. No new actionable documentation issues found.

2026-06-02 code-shaper review: PASS — read `.psi/skills/code-shaper/SKILL.md`, task artifacts, implementation, docs, and tests; reviewed source for simplicity, consistency, and robustness. Verified scoped formatting remains localized in `scry.capture`, invocation intent classification remains in `scry.clojure-test`, public helper fallbacks remain orthogonal in `scry.core`, and Kaocha adaptation reuses the shared formatter without crossing the core dependency boundary. Ran `clojure -M:test -e "(require 'scry.capture-test 'scry.clojure-test-test) (clojure.test/run-tests 'scry.capture-test 'scry.clojure-test-test)"` and `clj-kondo --lint src src-kaocha test` successfully. No new actionable code-shaping issues found.
