# Design follow-up steps

- [x] Clarify whether `actionlint` is required as a CI workflow step/job, or only as a local validation command run during implementation and documented in task notes/AGENTS.md.
