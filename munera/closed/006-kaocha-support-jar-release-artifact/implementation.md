# Implementation notes

No implementation yet. The task was created from the maintainer request to add a separate jar for Kaocha support and include that jar in release builds, Clojars deploys, and GitHub Release artifacts.

2026-06-03 architecture review: Design fits current architecture: it preserves the core/Kaocha dependency boundary, keeps `scry.core` independent of Kaocha, packages adapter code under the optional artifact, and extends build/release surfaces without changing runtime result semantics. META.md and doc/architecture.md are absent; review used AGENTS.md and design.md. No new actionable architectural-fit feedback.

2026-06-03 ambiguity review: Found three actionable ambiguities in design.md: the deploy command semantics are left as either changing existing `deploy` or adding deploy-all, the adapter pom's Kaocha dependency version source is not pinned, and the combined two-artifact build/deploy path does not specify how generated class/pom output is kept from clobbering between artifacts. Added follow-up items to design-steps.md.

2026-06-03 design ambiguity follow-up: Completed all three review-added design steps. Design now keeps `clojure -T:build:deploy deploy` as the core-only deploy command and requires a clearly named combined `deploy-all` task for release publishing. Design pins the adapter pom's Kaocha dependency version to the development `:kaocha` alias source, centralized or deterministically read by build code with focused test coverage for the current version. Design also requires isolated class/pom output directories for core and adapter artifacts (for example `target/classes` and `target/classes-kaocha`) and explicit retention of both jar/pom paths plus deploy order in combined build/deploy return values.

2026-06-03 inconsistency review: Reviewed design.md against build.clj, deps.edn, .github/workflows/release.yml, README.md, AGENTS.md, focused build/release tests, and referenced task 005 design. The design is internally consistent and its intended changes consistently supersede the current core-only release surface while preserving the core/Kaocha dependency boundary. No new actionable inconsistency feedback.

2026-06-03 plan ambiguity review: Found three actionable ambiguities in plan.md/steps.md: standalone `kaocha-jar` clean/stale-output semantics are not pinned, the adapter Kaocha dependency source-of-truth mechanism is left as read-or-centralize, and release workflow artifact resolution lacks exact non-overlapping core/adapter jar patterns. Added follow-up items to steps.md.

2026-06-03 plan ambiguity follow-up: Completed all three review-added planning steps without implementing the task body. `plan.md` now chooses deterministic `deps.edn` `:kaocha` alias reading as the single source of truth for the adapter pom's `lambdaisland/kaocha` version, with focused tests required to compare pom metadata against that alias so drift fails. It also pins standalone `clojure -T:build kaocha-jar` stale-output semantics: delete adapter-specific outputs only and preserve existing core jar output, while combined `jars` cleans all `target/` once. Release workflow artifact resolution is now specified with exact non-overlapping jar patterns: core `target/scry-[0-9]*.[0-9]*.[0-9]*.jar` excluding `scry-kaocha`, adapter `target/scry-kaocha-[0-9]*.[0-9]*.[0-9]*.jar`, and each lookup must fail unless exactly one jar is present.

2026-06-03 plan inconsistency review: Reviewed plan.md and steps.md against design.md, implementation.md, build.clj, deps.edn, .github/workflows/release.yml, focused build/release tests, AGENTS.md, and CHANGELOG.md. The task files are consistent: previous ambiguity follow-ups are reflected in both plan and checked steps, implementation slices preserve current core-only behavior while adding the adapter artifact, and remaining unchecked steps match the intended unimplemented task body. No new actionable inconsistency feedback.

2026-06-03 implementation pass: Implemented the task body across build, deploy, release workflow, tests, and docs. `build.clj` now has separate core and adapter artifact constants/paths, deterministic `deps.edn` `:kaocha` alias reading for the adapter `lambdaisland/kaocha` version, isolated `target/classes` and `target/classes-kaocha` pom/class output, `kaocha-jar`, combined `jars`, and `deploy-all` that deploys core before adapter. The adapter pom is generated deliberately rather than from the full project basis so it contains Maven dependencies on same-version `org.hugoduncan/scry` and the alias-pinned Kaocha version without copying core classes into the adapter jar. Release workflow now builds both jars, resolves exact non-overlapping core/adapter patterns, validates shared version, uses `deploy-all`, and attaches both jars. Focused build and release tests were updated for the two-artifact shape, deploy ordering, output isolation, and alias/pom drift prevention. README, AGENTS.md, and CHANGELOG.md document the optional adapter artifact.

2026-06-03 verification: Core scry tests passed (`45 tests, 169 pass, 0 fail, 0 error`), focused Kaocha adapter tests passed (`8 tests, 48 assertions`), focused build checks passed (`6 tests, 74 assertions`), focused release checks passed (`17 tests, 85 assertions`), and `clojure -T:build jars` produced both `target/scry-0.1.159.jar` and `target/scry-kaocha-0.1.159.jar`. Direct `actionlint` was not on PATH, so validation was run through the project tool manager with `mise exec -- actionlint`, which passed.

2026-06-03 implementation review: Reviewed build.clj, deps.edn, release workflow, focused build/release tests, README, AGENTS.md, and CHANGELOG.md against the task design/plan. The implementation preserves core-only jar/deploy semantics, keeps adapter outputs isolated, reads the adapter Kaocha dependency version from deps.edn, deploys core before adapter, and statically guards the two-artifact release workflow. Verification rerun: core tests, focused Kaocha tests, focused build checks, focused release checks, clojure -T:build jars, and mise exec -- actionlint all pass. No new actionable implementation-quality issues.

2026-06-03 test review: Reviewed focused build and release tests against design/plan acceptance criteria and inspected build/deploy/release workflow surfaces they exercise. Coverage includes core/adapter jar contents and negative boundaries, adapter pom dependency metadata and deps.edn alias drift prevention, output isolation, standalone/combined build return shapes, credential failures, deploy-all argument shape/order, two-artifact release workflow commands/patterns/attachments, and publishing gates. Infrastructure boundaries used by tests are injectable/nullable where needed (Git process and deps-deploy) without mocks/stubs. Focused build checks and focused release checks pass. No new actionable test-quality issues.

2026-06-03 test-shaper review: Found one actionable test-quality issue: the adapter pom dependency checks assert `<artifactId>scry</artifactId>` and the build version independently, so the same-version core dependency requirement could still pass from the adapter pom's top-level `<version>` even if the `org.hugoduncan/scry` dependency version is missing or wrong. Add dependency-scoped pom assertions/parsing for the adapter's core dependency.

2026-06-03 test-shaper follow-up: Strengthened focused adapter pom checks in `test/scry/build_test.clj` by parsing generated `<dependency>` blocks and asserting the `org.hugoduncan/scry` dependency map includes the build version in the same dependency entry. The Kaocha dependency assertion now uses the same dependency-scoped parser against the `deps.edn` `:kaocha` alias version. Focused build checks pass (`6 tests, 71 assertions`).

2026-06-03 test-shaper review: Found one actionable test-quality issue: the planned standalone `kaocha-jar` stale-output contract says adapter-specific stale outputs are deleted while existing core jars are preserved, but focused build coverage only asserts core preservation and does not create/assert removal of a stale `target/scry-kaocha-*.jar`. Add a focused test for stale adapter jar removal so the standalone command cannot silently leave multiple adapter artifacts.

2026-06-03 test-shaper follow-up: Added focused build coverage for standalone `kaocha-jar` stale-output semantics. `kaocha-jar-build-produces-adapter-artifact-test` now creates a stale `target/scry-kaocha-0.0.stale.jar` after producing an existing core jar, runs standalone `kaocha-jar`, and asserts the stale adapter jar is removed while the existing core jar is preserved. Focused build checks pass (`6 tests, 72 assertions`).

2026-06-03 test-shaper review: Re-reviewed focused build and release tests after the stale adapter jar follow-up. Coverage is locally comprehensible and behavior-focused for the core/adapter artifact boundary, dependency-scoped adapter pom metadata, standalone stale-output semantics, deploy-all ordering, and two-artifact release workflow guards. Focused build checks and focused release checks pass. No new actionable test-quality issues.

2026-06-03 docs review: Reviewed README.md, CHANGELOG.md, AGENTS.md, design/plan/steps, build.clj, deps.edn, release workflow, and focused build/release tests for the optional Kaocha adapter artifact and two-jar release behavior. User-facing README documents the separate `org.hugoduncan/scry-kaocha` opt-in coordinate and keeps runtime examples accurate; CHANGELOG.md records the user-visible artifact/release change; no doc/ directory is present. No new actionable documentation issues.

2026-06-03 code-shaper review: Found one actionable robustness issue in `build.clj`: `deps-edn` parses `deps.edn` with Clojure `read-string` even though the file is EDN data. Use `clojure.edn/read-string` instead so the deterministic Kaocha alias metadata reader stays data-only and locally aligned with the dependency-source-of-truth invariant.

2026-06-03 code-shaper follow-up: Replaced `build.clj` `deps.edn` parsing via Clojure `read-string` with `clojure.edn/read-string`, keeping the Kaocha alias dependency metadata reader data-only. Focused build checks pass (`6 tests, 72 assertions`).

2026-06-03 code-shaper review: Found one new actionable consistency/robustness issue in `test/scry/build_test.clj`: the focused test helper still parses `deps.edn` with Clojure `read-string`, while production build code now uses `clojure.edn/read-string` for the same data-only dependency-source-of-truth path. Align the test helper with EDN parsing so the review-added invariant is consistently enforced in tests and production.

2026-06-03 code-shaper follow-up: Replaced `test/scry/build_test.clj` `deps.edn` parsing via Clojure `read-string` with `clojure.edn/read-string`, aligning focused tests with the production build helper's data-only EDN parser for the Kaocha alias dependency metadata path. Focused build checks pass (`6 tests, 72 assertions`).

2026-06-03 code-shaper review: Re-reviewed build.clj, focused build tests, release workflow/static checks, README, AGENTS.md, and CHANGELOG.md after the EDN-parser follow-up. The build/release code is locally comprehensible, keeps core/adapter responsibilities separated, uses consistent data-only deps.edn parsing, preserves output/deploy ordering invariants, and focused build checks pass (`6 tests, 72 assertions`). No new actionable code-quality issues.
