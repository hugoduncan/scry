# Implementation notes

No implementation yet. Created for the public-readiness pass before making the repository public.

Architecture review (2026-06-04): no new actionable architectural-fit feedback. The design keeps installation docs in README as user-facing material, uses the established core (`org.hugoduncan/scry`) and optional Kaocha (`org.hugoduncan/scry-kaocha`) artifact boundary with same-version guidance, and respects Git-count release versioning by requiring a non-conflicting placeholder/current version. Consulted AGENTS.md and README.md; META.md and doc/architecture.md were absent.

Ambiguity review (2026-06-04): found actionable ambiguities in the installation design: where dependency snippets should live (`:deps` vs a test/dev alias), the exact version token/value needed for copyable examples while preserving Git-count release semantics, and the adapter example's dependency composition/transitive expectations. Added follow-up items to `design-steps.md`. Consulted README.md, build.clj, deps.edn, CHANGELOG.md, AGENTS.md, and the task design.

Design follow-up execution (2026-06-04): completed the ambiguity-review items in `design-steps.md`. Updated `design.md` to choose test/development aliases for README installation snippets, keep command examples aligned with `:test` / `:test:kaocha`, pin copyable snippet versions to `"RELEASE"` with explanatory `0.1.<git-count>` / `0.1.N` release-version text, and clarify that optional Kaocha docs should list `org.hugoduncan/scry-kaocha` explicitly at the same version as core while relying on adapter transitive dependencies for core and Kaocha unless users intentionally override them. Marked all newly added design follow-up items complete.

Inconsistency review (2026-06-04): no new actionable inconsistency feedback. The design is internally consistent after the ambiguity follow-up: README installation snippets use test/development aliases aligned with the documented `clojure -M:test` / `-X:test` and `:test:kaocha` commands, `RELEASE` is consistently a copyable placeholder with concrete Git-count `0.1.N` pinning guidance, and the optional adapter transitive-dependency claims match `build.clj`/AGENTS.md. Consulted README.md, deps.edn, build.clj, AGENTS.md, CHANGELOG.md, and the task design.

Planning refresh (2026-06-04): created/refreshed `plan.md` and `steps.md` from the stable design after ambiguity and inconsistency follow-ups. The plan is README-only and slices work through core installation docs, optional Kaocha installation docs, command/version consistency, and documentation verification. No blocking design ambiguities remain; non-blocking risks are `RELEASE` reproducibility wording, preserving the optional Kaocha boundary, and avoiding duplication with the existing Kaocha adapter section.

Plan ambiguity review (2026-06-04): found one actionable ambiguity. The plan pins the dependency snippet to `:aliases {:test {:extra-deps ...}}` and commands to `clojure -M:test` / `-X:test`, but does not say whether the public copyable `:test` alias should include classpath entries such as `:extra-paths ["test"]` or explicitly leave that as project-specific setup; without that, users may copy an alias that installs scry but does not put tests on the classpath. Added a follow-up item to `steps.md`. Consulted plan.md, steps.md, implementation.md, design.md, README.md, deps.edn, build.clj, CHANGELOG.md, and AGENTS.md.

Plan ambiguity follow-up execution (2026-06-04): completed the review-added `:test` alias classpath clarification item in `steps.md`. Chose to include the conventional `:extra-paths ["test"]` in the public copyable core installation snippet so the documented `clojure -M:test` / `clojure -X:test` commands have a test classpath by default, while adding plan/step wording that projects should adjust or merge classpath setup for existing/custom aliases. No README implementation items from before the review pass were executed in this follow-up.

Plan inconsistency review (2026-06-04): found one actionable inconsistency. `plan.md` and `steps.md` now require the public copyable core `:test` alias to include `:extra-paths ["test"]`, but `design.md` still shows the core-only snippet as `:aliases {:test {:extra-deps ...}}` without the test path; added a follow-up item to align or explicitly abbreviate the design example. Consulted design.md, plan.md, steps.md, implementation.md, README.md, deps.edn, build.clj, and AGENTS.md.

Plan inconsistency follow-up execution (2026-06-04): completed the review-added design alignment item in `steps.md`. Updated `design.md` so the core-only README installation snippet includes the plan-approved conventional `:extra-paths ["test"]` alongside `org.hugoduncan/scry` under the `:test` alias, with text telling projects to adjust or merge project-specific test classpath setup as needed. This was a documentation/task-artifact-only follow-up; no code or README implementation items from before the review pass were executed.

README implementation pass (2026-06-04): added a public `Installation` section after `Why scry?` and before `Usage`. The new docs include a core `:test` alias with `:extra-paths ["test"]` and `org.hugoduncan/scry` at `"RELEASE"`, command examples using `clojure -M:test` / `clojure -X:test`, concrete Git-count version pinning guidance for reproducible builds, and an optional composable `:kaocha` alias with `org.hugoduncan/scry-kaocha` at the same `"RELEASE"` token. The optional adapter text states that `scry-kaocha` brings same-version core and Kaocha transitively unless users deliberately manage/override Kaocha themselves. Documentation-only verification: reread README Installation, Command-line usage, and Kaocha adapter sections; ran `git diff --check`; grepped README dependency snippets to confirm all `:mvn/version` values are exactly `"RELEASE"`. No code or tests were changed.

Implementation review (2026-06-04): no new actionable implementation-quality feedback. README Installation matches the task design and plan: core and optional Kaocha dependency snippets use test/development aliases, exact `"RELEASE"` tokens, aligned `:test` / `:test:kaocha` commands, Git-count concrete-version pinning guidance, and optional adapter transitive-dependency guidance. No code, abstraction, architecture-boundary, or performance concerns found for this README-only change. Consulted design.md, plan.md, steps.md, implementation.md, README.md, build.clj, deps.edn, CHANGELOG.md, and AGENTS.md; reran `git diff --check` and README version-token grep.

Test review (2026-06-04): no new actionable test-quality feedback. This is a README-only documentation change, so the relevant verification is documentation-focused rather than new code tests. Rechecked the README installation snippets as parseable EDN, confirmed all README `:mvn/version` snippets use exactly `"RELEASE"`, reran `git diff --check`, and compared the documented core/Kaocha dependency claims against `deps.edn`, `build.clj`, and focused build-test coverage for adapter POM dependencies.

Test-shaper review (2026-06-04): no new actionable test-quality feedback. Reviewed the README-only installation change through clarity, signal, robustness, and economy: the relevant verification stays documentation-focused, snippets are locally comprehensible and parseable EDN, all dependency examples use the exact `"RELEASE"` token, command examples align with the documented aliases, and dependency/transitive claims are supported by `build.clj`, `deps.edn`, and focused build-test coverage for adapter POM metadata. Reran `git diff --check`, README version-token grep, and README install-snippet EDN parsing.

Docs review (2026-06-04): no new actionable documentation feedback. README Installation covers core and optional Kaocha dependency snippets, aligned `:test` / `:test:kaocha` commands, exact `"RELEASE"` tokens with concrete `0.1.N` pinning guidance, and adapter transitive-dependency expectations; existing CLI/Kaocha README examples remain consistent. No `doc/` directory is present. CHANGELOG.md already documents the underlying public artifacts/versioning, and this README-only installation-doc addition does not introduce a new runtime behavior requiring a changelog entry. Verification: reread README Installation, Command-line usage, Kaocha adapter, and CHANGELOG.md; parsed the Installation EDN snippets with `clojure.edn/read-string`; reran `git diff --check` and README version-token grep.

Code-shaper review (2026-06-04): no new actionable code-quality feedback. The README-only installation change is locally comprehensible and low-coupling: dependency snippets are simple EDN, command examples align with the documented `:test` / `:test:kaocha` aliases, version-token guidance is consistent, and optional Kaocha wording preserves the core/adapter boundary described by `build.clj`, `deps.edn`, and build-test coverage. Verification: reread task artifacts, README Installation/CLI/Kaocha sections, build.clj, deps.edn, CHANGELOG.md, and test/scry/build_test.clj; reran `git diff --check`, README version-token grep, Installation EDN snippet parsing, and focused build checks.
