# Design follow-up steps

- [x] Decide and record the intended public maturity label/guardrails for the README status wording (for example alpha/pre-1.0 versus initial public release) so implementation has a clear target instead of the current conditional "if that remains the maintainer intent."
- [x] Align the acceptance criteria with the scoped surface list so the README status must accurately reflect the documented core, CLI, build/release, nested capture, and optional Kaocha surfaces, not only the subset of core/CLI/Kaocha.
