# Implementation Notes

Task created on 2026-09-08. No implementation has started.

Design orientation inspected the existing core completed-var callback, CLI end-of-run writer and robust sanitizer/diagnostic boundary, pinned Kaocha 1.91.1392 reporter/plugin lifecycle, and prior tasks 007 and 025. The design resolves timing, payload, runner parity, atomic publication, reconciliation, duplicate callbacks, mutable snapshots, diagnostic failures, runner aborts, synthetic entries, and durability limits.

- no architectural review feedback
- ambiguity review added 6 new design steps
- inconsistency review added 3 new design steps
- Follow-up resolution should preserve runner authority and keep diagnostic I/O non-authoritative; use `src/scry/cli.clj` for outcome/progress composition, `src/scry/cli/results.clj` for naming/sanitization/filesystem lifecycle, `src/scry/clojure_test.clj` for core callback timing, `src-kaocha/scry/kaocha.clj` for hook ordering, and the public `scry.cli/run` contract generated into `doc/API.md` when settling exact semantics.

2026-09-08 — Design-review follow-up: Kaocha executes plugin hooks left-to-right (`plugin/run-hook*` reduces the active chain), capture-output materializes and removes its buffer in `post-test`, and `run-testables` starts the next leaf only after the current leaf’s full `post-test` chain returns. The adapter completion plugin therefore needs to be appended last after the final configured/ensured plugin chain, not merely placed after capture-output. Existing CLI terminal progress writes/flushes synchronously and does not contain writer failures; the refined design deliberately preserves that behavior while containing sink failures only.

- 2026-09-08 architectural re-review found no new design feedback
- 2026-09-08 ambiguity re-review found no new design feedback
- 2026-09-08 inconsistency re-review found no new design feedback
- 2026-09-08 follow-up handoff: all nine design steps are resolved in `design.md`; regression homes are `test/scry/cli_test.clj`, `test/scry/clojure_test_test.clj`, `test/scry/kaocha_test.clj`, and `test/scry/cli_kaocha_test.clj`. Verify duplicate executions, publication timing, and diagnostic ordering as observable state behavior rather than private call order.
- 2026-09-08 plan ambiguity review added 3 new design steps
- 2026-09-08 plan inconsistency review added 3 new design steps
- 2026-09-08 plan-review handoff: resolve exception preservation by lifecycle phase rather than inheriting `run-cli`’s current broad `try`/`catch`; preserve callback-generation identity when selecting reconciliation retry data; and reserve callback-known paths before synthetic assignment. Relevant seams are `run-cli`, `diagnostic-error`, and `write-result-files-diagnostic!` in `src/scry/cli.clj`, plus `result-file-assignments` in `src/scry/cli/results.clj`.
- 2026-09-08 plan-review follow-up resolution: canonical-vector validation is the reconciliation boundary—runner/validation exceptions snapshot incremental state, while reconciliation runs once immediately after validation and later exceptions preserve that reconciled state. Duplicate callbacks match same-var canonical occurrences ordinally; unmatched required generations retry their retained callback snapshot. Synthetic assignment must reserve callback-only concrete paths with successful or unresolved artifact state. Catch-path rendering must be best-effort so an output failure cannot erase the primary structured outcome. Slice 6 is complete only after one green focused Kaocha slice plus 10 consecutive bounded interruption regressions (or 10 deterministic fallback regressions with the platform limitation recorded).
- 2026-09-08 plan ambiguity re-review found no new actionable feedback
- 2026-09-08 plan inconsistency re-review added 2 new design steps
- 2026-09-08 re-review follow-up handoff: resolve diagnostic phase semantics at the canonical validation/reconciliation boundary and explicitly cover reconciliation-orchestration failure; keep callback occurrence ordinals (advanced by every concrete callback) separate from required/published artifact generations (advanced only by failures/errors). Relevant implementation and regression seams are `canonical-result-entries`/`run-cli` in `src/scry/cli.clj`, the new sink state in `src/scry/cli/results.clj`, and duplicate/reconciliation cases in `test/scry/cli_test.clj`.
- 2026-09-08 plan re-review follow-up resolution: runner return is the diagnostic-phase boundary required by `design.md`: only a runner that throws before return uses `:incremental-result-file-writing`; missing/malformed canonical data after a normal return and any reconciliation-orchestration failure use `:final-result-file-reconciliation`. Canonical validation remains the separate gate for whether reconciliation may start. Sink state must also track every concrete callback's per-identity occurrence ordinal independently from the failure-only required/published generation, so pass/unknown executions align later duplicate canonical occurrences without requiring artifacts.
- 2026-09-08 plan ambiguity batch review found no new actionable feedback
- 2026-09-08 plan inconsistency batch review found no new actionable feedback
- 2026-09-08 design-step handoff: no unchecked design steps remain; treat the checked items as resolved rationale and implement the consolidated contracts in `design.md`, `plan.md`, and `steps.md` rather than reopening earlier alternatives.
- 2026-09-08 Slice 1: baseline core command (`scry.capture-test`, `scry.clojure-test-test`, `scry.cli-test`) passed 145 tests/779 assertions; optional Kaocha command passed 33 tests/173 assertions. `scry.clojure-test-test` now establishes that callbacks are ordered full canonical entries after `:each` teardown, including setup/body/teardown output and assertions. The core runner already met this contract; only its public callback docstring was clarified. Focused runner verification passed 65 tests/159 assertions; `bb clj-fmt:check` passed. Committed as `978de4a` (`Characterize core progress callback completion`).
- 2026-09-08 Slice 2: Added `create-result-sink` / `handle-completed-entry!` in `scry.cli.results`. The sink advances every concrete callback occurrence, creates failure-only generations, atomically publishes sanitized entries via a same-directory temporary path and `ATOMIC_MOVE`/`REPLACE_EXISTING`, and records publication exceptions instead of escaping callbacks. It is not wired into `run-cli` yet; reconciliation and result-file ordering remain later slices. Focused `scry.cli-test` passed 78 tests/620 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 2 follow-up: A direct atomic publisher boundary regression verifies the final path is absent while the temporary file is serialized, and a forced serialization failure leaves neither final nor sink temporary file. Focused `scry.cli-test` passed 79 tests/627 assertions. Next slice can build reconciliation state atop the existing contained publication errors; no private ordering assertions were added.
- 2026-09-08 Slice 3 (partial): `scry.cli.results/reconcile-result-sink!` now retries an unresolved callback generation from its ordinally matching canonical occurrence, falls back to retained callback data, writes callback-missed concrete failures and synthetic failures, and reports deduplicated successful paths plus unresolved state. `sink-exception-snapshot` preserves only callback-time successful paths for runner-abort handling. `result-file-assignments` accepts optional concrete filename reservations so synthetic reconciliation cannot collide with callback-known artifacts. Added state-based sink tests for retry/callback omission/synthetic output and exception snapshots; focused `scry.cli-test` passed 81 tests / 635 assertions and `bb clj-fmt:check` passed. CLI integration, full diagnostic ordering, duplicate-specific cases, and cleanup remain.
- 2026-09-08 Slice 3: Added a state-based regression that publishes one concrete failure, observes a later pass for the same var, then reconciles. The artifact remains the original completion snapshot and the publisher is invoked once, establishing that normal reconciliation does not rewrite an already successful required generation. Focused `scry.cli-test` passed 82 tests/639 assertions; `bb clj-fmt:check` passed. The next slice should cover unresolved ordering/diagnostic metadata and cleanup before CLI lifecycle integration.
- 2026-09-08 Slice 4 (partial): `run-cli` now creates the CLI-owned sink after result-directory preparation and composes it before terminal progress. It reconciles after a normal runner return, preserves published callback files on catchable runner errors, and selects incremental versus final-reconciliation diagnostic phase at the runner-return boundary. Sink unresolved ordering is canonical-first, callback-only completion order, then synthetic. The obsolete end-only `:write-result-files` injection coverage was removed in favour of sink behavior; focused `scry.cli-test` passed 73 tests/545 assertions and `bb clj-fmt:check` passed. Remaining Slice 4 work needs direct timing, runner-error, progress-writer, and persistent/transient publication-failure integration tests.
- 2026-09-08 Slice 4 follow-up: Added CLI-level, state-based integration coverage using injected runner/sink boundaries: a following execution reads the first failure's fully detailed EDN before it proceeds, a pass creates no file, a catchable runner exception preserves and reports an already-published artifact, and a first contained publication failure is retried by reconciliation without changing the test-failure outcome. Focused `scry.cli-test` passed 76 tests/558 assertions; `bb clj-fmt:check` (including lint) passed. Committed as `276b469` (`Test incremental CLI failure publication`). The next slice should test persistent publication failure diagnostics plus canonical-validation/reconciliation/output exception lifecycle boundaries; these tests intentionally exercise visible files/outcomes rather than callback call counts.
- 2026-09-08 Slice 4 diagnostic follow-up: Added state-based regressions for callback-only concrete filename reservation against a synthetic collision, persistent publication failure retaining a successful sibling with `:final-result-file-reconciliation` diagnostic metadata, and malformed canonical data after a normal runner return retaining final-phase unresolved diagnostic state without reconciliation. Focused `scry.cli-test` passed 79 tests/573 assertions; `bb clj-fmt:check` passed. Remaining lifecycle work is reconciliation-orchestration/post-output exception coverage and the runner-error case that combines unresolved incremental publication with a surviving sibling artifact.
- 2026-09-08 Slice 3 duplicate reconciliation follow-up: Added a state-based sink regression that forces the second duplicate failure publication to fail. Reconciliation retries from its ordinally matching second canonical entry when present; if that occurrence is absent, it retries the retained completion snapshot. A later pass advances only occurrence alignment and creates no requirement. Focused `scry.cli-test` passed 80 tests/579 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 4 runner-abort diagnostic follow-up: Added an injected-runner regression with one contained unpublished failure and one published sibling before a runner exception. The runner-error outcome retains only the readable sibling path, reports the unresolved identity under `:incremental-result-file-writing`, and writes bounded diagnostic details before the ordinary runner-error and results-directory pointer. Focused `scry.cli-test` passed 81 tests/587 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 4 lifecycle follow-up: Added a narrow reconciliation boundary injection to prove a post-return reconciliation orchestration exception neither retries publication nor changes the diagnostic phase from `:final-result-file-reconciliation`. A summary-writer exception after a callback-published/reconciled failure retains the final artifact in the runner-error outcome. Focused `scry.cli-test` passed 82 tests/594 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 4 progress/missing-canonical follow-up: A normal runner return without `:canonical-results` now has regression coverage showing that a callback-published artifact remains listed with no unnecessary sink diagnostic. A terminal progress writer failure is separately covered as a runner error without `:scry.cli/diagnostic-error`, while the sink-first artifact survives. Focused `scry.cli-test` passed 83 tests/602 assertions; focused core REPL slice (`scry.clojure-test-test`, `scry.cli-test`) passed 77 tests/761 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 4 native timing follow-up: Added `scry.fixtures.incremental-cli`, where the second real `clojure.test` var reads the first failing var's artifact. The regression proves native CLI publication occurs after `:each` teardown and before the next var body, retaining setup/body/teardown output and assertion detail. Focused `scry.cli-test` passed 86 tests/607 assertions; `bb clj-fmt:check` passed.
- 2026-09-08 Slice 5 (partial): Added the optional adapter-owned `:scry.kaocha/completed-entry` plugin. Its final `post-test` hook synchronously sends full `testable->entry` data for non-skipped leaves, after every configured hook and capture-output; the former reporter now reports synthetic errors only. `testable->entry` defensively reads a still-present capture buffer when finalized output is unavailable. Focused `scry.kaocha-test` passed 22 tests/99 assertions, focused `scry.cli-kaocha-test` passed 11 tests/78 assertions, and `bb clj-fmt:check` passed. Remaining Slice 5 work: fixture teardown, user-hook ordering, skips, and synthetic-error regressions.
- 2026-09-08 Slice 5 follow-up: Added a narrow state-based regression for `completed-entry-post-test`: one non-skipped leaf emits exactly one canonical callback and returns the identical testable, while skipped leaves and group nodes emit nothing. Focused `scry.kaocha-test` passed 23 tests/105 assertions; `bb clj-fmt:check` passed. User-hook ordering and integration fixture timing remain separate work.
- 2026-09-08 Slice 5 completion follow-up: Added real temporary-project adapter coverage for a failing leaf with an `:each` fixture. Its single callback equals the final canonical entry and includes merged setup/body/teardown output with empty `:err`. A configured user `post-test` hook that adds seven pass counts is visible in both callback and final conversion, proving the adapter observer is last. Synthetic reporter coverage confirms only non-var error events emit a synthetic callback. Focused `scry.kaocha-test` passed 26 tests/116 assertions.
- 2026-09-08 Slice 5 finalization: A real broken temporary Kaocha namespace now proves that a load error creates exactly one synthetic nil-var callback and no canonical concrete leaf, preserving synthetic final-reconciliation ownership. Focused adapter and CLI-Kaocha checks passed (26 tests/121 assertions; 11 tests/78 assertions); `bb clj-fmt:check` and `bb clj-kondo:lint` passed. Slice 5 is complete; Slice 6 needs end-to-end incremental Kaocha artifact timing and bounded interruption coverage.
- 2026-09-08 Slice 6 timing: A real temporary Kaocha project now has its second leaf read the first failing leaf’s artifact from `.scry-results/` before the second body continues. The readable atomic EDN retains assertions and merged `:each` setup/body/stderr/teardown output with empty `:err`. The temporary-project helper now makes `clojure.edn`, `clojure.java.io`, and `use-fixtures` available to generated namespaces. Focused `scry.cli-kaocha-test` passed 12 tests/85 assertions; `bb clj-fmt:check` passed. Next slice needs synthetic reconciliation and bounded interruption coverage.
- 2026-09-08 Slice 6 synthetic reconciliation: Added a real broken-Kaocha-project CLI regression using an observing stderr writer. The synthetic `suite-error-1` live progress label occurs while its final EDN path is absent; normal final reconciliation then creates the readable synthetic error artifact. Focused `scry.cli-kaocha-test` passed 13 tests/89 assertions; `bb clj-fmt:check` and `bb clj-kondo:lint` passed. Bounded interruption coverage remains.
- 2026-09-08 Slice 6 blocked-run fallback: Used deterministic same-process latch synchronization rather than child-process termination because it directly observes the required readable artifact during the later leaf's active block without platform-specific process control. The latch has bounded readiness/completion waits and is always released in `finally`; the fallback regression passed 10 consecutive executions with no sleeps. The generated blocked leaf is intentionally assertion-free, so Kaocha reports it as a failure/unknown-style non-passing leaf under the current adapter, but the test asserts only the first completed failure artifact and successful cleanup. Focused verification passed: `scry.kaocha-test` 26 tests/121 assertions, `scry.cli-kaocha-test` 14 tests/98 assertions; `bb clj-fmt:check` and `bb clj-kondo:lint` passed.
- 2026-09-08 Slice 4 lifecycle follow-up: `reconcile-result-sink!` now stores the single completed reconciliation snapshot in sink state, so a later presentation exception preserves synthetic artifacts as well as callback-backed paths without a second reconciliation attempt. A state-based summary-writer failure regression verifies the synthetic artifact remains readable and listed. Focused `scry.cli-test` passed 87 tests/610 assertions; `bb clj-fmt:check` passed.
- 2026-09-09 Slice 4 complete: seed-writer failure coverage confirms that a presentation exception after summary and reconciliation retains the already-published detailed file in the runner-error snapshot. Focused `scry.cli-test` passed 87 tests/613 assertions; `scry.clojure-test-test` passed 65 tests/159 assertions; `bb test:core`, `bb clj-fmt:check`, and `bb clj-kondo:lint` passed. Committed as `3f57ca3` (`Complete CLI sink lifecycle coverage`). Subsequent compatibility checks should retain the distinction that sink I/O is contained but output writer failures are runner errors.
- 2026-09-09 Slice 7: all compatibility slices passed via `bb test` (core 85 tests/792 assertions; Kaocha adapter 26/121; Kaocha CLI 14/98; build 7/184; release 17/85), followed by formatting and lint. Direct failing core `-m` and `-X` invocations both exited 1 and left a readable atomic final EDN artifact; a core-only namespace-load command passed without the optional adapter alias. Existing JVM reflection warnings and intentional unowned-thread fixture diagnostics remain non-failing baseline output. Documentation/API regeneration and final task verification remain Slice 8.
- 2026-09-09 Slice 8 (documentation): README, CHANGELOG, and AGENTS now document synchronous atomic final-file publication, reconciliation, durability limits, runner-error path preservation, and diagnostic phases. `scry.cli/run` and the Kaocha callback docstrings provide the public completed-entry contract; the API-doc generator override required the same CLI wording update. `bb api-docs`, `bb api-docs --check`, the focused API-doc regression (65 assertions), formatting, and lint all passed. Remaining Slice 8 work is the documented command-line/final suite verification.
- 2026-09-09 Slice 8 final verification: focused command-line checks passed — `scry.cli-test` 87 tests/613 assertions, `scry.kaocha-test` 26/121, and `scry.cli-kaocha-test` 14/98. Direct failing core (`-m --var scry.fixtures.failing/equality-fails`) and Kaocha (explicit inline `:config`, `failure` suite) CLI runs both exited 1 and produced readable detailed final EDN artifacts. `bb test:core`, `bb test:kaocha`, and `bb test` passed (full: core 85/792, adapter 26/121, Kaocha CLI 14/98, build 7/184, release 17/85); `bb clj-fmt:check`, `bb clj-kondo:lint` (zero findings), and `bb api-docs --check` passed. Final design acceptance/out-of-scope review found no deviations. Task artifacts are committed as `f603857` (`Verify incremental CLI artifact task`).
- 2026-09-09 implementation review added 1 step to be addressed.
- addressed 1 review step; focused Kaocha adapter (26 tests/125 assertions), Kaocha CLI (15/103), format, and lint checks passed.
- added 1 step to be addressed.
- addressed 1 review step.
- added 1 step to be addressed.
- addressed 1 review step.
- added 1 step to be addressed.
- review found no new actionable issues; corrected 7 stale unchecked steps.
- test review added 4 steps to be addressed.
- addressed 4 review steps.
- 2026-09-09 test re-review found no new actionable issues.
- test review added 3 steps to be addressed.
- addressed 3 review steps; focused Kaocha adapter (26 tests/127 assertions), Kaocha CLI (16/118), format, and lint checks passed.
