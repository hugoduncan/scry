# Implementation Notes

Task created on 2026-09-08. No implementation has started.

Design orientation inspected the existing core completed-var callback, CLI end-of-run writer and robust sanitizer/diagnostic boundary, pinned Kaocha 1.91.1392 reporter/plugin lifecycle, and prior tasks 007 and 025. The design resolves timing, payload, runner parity, atomic publication, reconciliation, duplicate callbacks, mutable snapshots, diagnostic failures, runner aborts, synthetic entries, and durability limits.

- no architectural review feedback
- ambiguity review added 6 new design steps
- inconsistency review added 3 new design steps
- Follow-up resolution should preserve runner authority and keep diagnostic I/O non-authoritative; use `src/scry/cli.clj` for outcome/progress composition, `src/scry/cli/results.clj` for naming/sanitization/filesystem lifecycle, `src/scry/clojure_test.clj` for core callback timing, `src-kaocha/scry/kaocha.clj` for hook ordering, and the public `scry.cli/run` contract generated into `doc/API.md` when settling exact semantics.

2026-09-08 — Design-review follow-up: Kaocha executes plugin hooks left-to-right (`plugin/run-hook*` reduces the active chain), capture-output materializes and removes its buffer in `post-test`, and `run-testables` starts the next leaf only after the current leaf’s full `post-test` chain returns. The adapter completion plugin therefore needs to be appended last after the final configured/ensured plugin chain, not merely placed after capture-output. Existing CLI terminal progress writes/flushes synchronously and does not contain writer failures; the refined design deliberately preserves that behavior while containing sink failures only.

- 2026-09-08 architectural re-review found no new design feedback
- 2026-09-08 ambiguity re-review found no new design feedback
- 2026-09-08 inconsistency re-review found no new design feedback
- 2026-09-08 follow-up handoff: all nine design steps are resolved in `design.md`; regression homes are `test/scry/cli_test.clj`, `test/scry/clojure_test_test.clj`, `test/scry/kaocha_test.clj`, and `test/scry/cli_kaocha_test.clj`. Verify duplicate executions, publication timing, and diagnostic ordering as observable state behavior rather than private call order.
- 2026-09-08 plan ambiguity review added 3 new design steps
- 2026-09-08 plan inconsistency review added 3 new design steps
- 2026-09-08 plan-review handoff: resolve exception preservation by lifecycle phase rather than inheriting `run-cli`’s current broad `try`/`catch`; preserve callback-generation identity when selecting reconciliation retry data; and reserve callback-known paths before synthetic assignment. Relevant seams are `run-cli`, `diagnostic-error`, and `write-result-files-diagnostic!` in `src/scry/cli.clj`, plus `result-file-assignments` in `src/scry/cli/results.clj`.
- 2026-09-08 plan-review follow-up resolution: canonical-vector validation is the reconciliation boundary—runner/validation exceptions snapshot incremental state, while reconciliation runs once immediately after validation and later exceptions preserve that reconciled state. Duplicate callbacks match same-var canonical occurrences ordinally; unmatched required generations retry their retained callback snapshot. Synthetic assignment must reserve callback-only concrete paths with successful or unresolved artifact state. Catch-path rendering must be best-effort so an output failure cannot erase the primary structured outcome. Slice 6 is complete only after one green focused Kaocha slice plus 10 consecutive bounded interruption regressions (or 10 deterministic fallback regressions with the platform limitation recorded).
- 2026-09-08 plan ambiguity re-review found no new actionable feedback
- 2026-09-08 plan inconsistency re-review added 2 new design steps
- 2026-09-08 re-review follow-up handoff: resolve diagnostic phase semantics at the canonical validation/reconciliation boundary and explicitly cover reconciliation-orchestration failure; keep callback occurrence ordinals (advanced by every concrete callback) separate from required/published artifact generations (advanced only by failures/errors). Relevant implementation and regression seams are `canonical-result-entries`/`run-cli` in `src/scry/cli.clj`, the new sink state in `src/scry/cli/results.clj`, and duplicate/reconciliation cases in `test/scry/cli_test.clj`.
- 2026-09-08 plan re-review follow-up resolution: runner return is the diagnostic-phase boundary required by `design.md`: only a runner that throws before return uses `:incremental-result-file-writing`; missing/malformed canonical data after a normal return and any reconciliation-orchestration failure use `:final-result-file-reconciliation`. Canonical validation remains the separate gate for whether reconciliation may start. Sink state must also track every concrete callback's per-identity occurrence ordinal independently from the failure-only required/published generation, so pass/unknown executions align later duplicate canonical occurrences without requiring artifacts.
- 2026-09-08 plan ambiguity batch review found no new actionable feedback
- 2026-09-08 plan inconsistency batch review found no new actionable feedback
- 2026-09-08 design-step handoff: no unchecked design steps remain; treat the checked items as resolved rationale and implement the consolidated contracts in `design.md`, `plan.md`, and `steps.md` rather than reopening earlier alternatives.
- 2026-09-08 Slice 1: baseline core command (`scry.capture-test`, `scry.clojure-test-test`, `scry.cli-test`) passed 145 tests/779 assertions; optional Kaocha command passed 33 tests/173 assertions. `scry.clojure-test-test` now establishes that callbacks are ordered full canonical entries after `:each` teardown, including setup/body/teardown output and assertions. The core runner already met this contract; only its public callback docstring was clarified. Focused runner verification passed 65 tests/159 assertions; `bb clj-fmt:check` passed. Committed as `978de4a` (`Characterize core progress callback completion`).
- 2026-09-08 Slice 2: Added `create-result-sink` / `handle-completed-entry!` in `scry.cli.results`. The sink advances every concrete callback occurrence, creates failure-only generations, atomically publishes sanitized entries via a same-directory temporary path and `ATOMIC_MOVE`/`REPLACE_EXISTING`, and records publication exceptions instead of escaping callbacks. It is not wired into `run-cli` yet; reconciliation, result-file ordering, and injected publisher cleanup coverage remain later slices. Focused `scry.cli-test` passed 78 tests/620 assertions; `bb clj-fmt:check` passed.
