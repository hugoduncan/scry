# Munera Plan

## Open

1. `024-cli-error-path-minimal-summary` — emit a minimal stdout summary on
   error/exception CLI outcomes instead of leaving `:summary nil` silent on
   stdout. Design drafted; not yet planned.
