# Munera Plan

## Open

1. `023-kaocha-cli-forward-m-argv` — forward raw `-m` Kaocha arguments to
   Kaocha's own parser; remove `--focus`/`--kaocha-opt` and the `-m`
   positional-suite collapse. Design complete; planning next.
2. `024-cli-error-path-minimal-summary` — emit a minimal stdout summary on
   error/exception CLI outcomes instead of leaving `:summary nil` silent on
   stdout. Design drafted; not yet planned.
